import Link from"next/link";import{AuthFields,AuthShell,FormNotice}from"@/features/authentication/components/AuthForm";import{signUpAction}from"@/features/authentication/actions/auth.actions";export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <AuthShell title="Create your account" description="Start a secure Vayon OS workspace for your organization."><FormNotice {...q}/><form action={signUpAction}><AuthFields kind="signup"/></form><p className="mt-5 text-center text-sm text-vds-muted">Already registered? <Link href="/login" className="text-vds-primary">Sign in</Link></p></AuthShell>}
