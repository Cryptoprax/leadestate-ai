"use client";
import { Button } from "@/features/platform/design-system";export default function Error({reset}:{error:Error;reset:()=>void}){return <div className="mx-auto max-w-xl p-16 text-center"><h1 className="text-xl font-semibold">Integration control plane unavailable</h1><p className="mt-3 text-sm text-vds-muted">No provider action was performed.</p><Button variant="primary" className="mt-6 rounded-xl px-4 py-3 text-sm font-semibold" onClick={reset}>Retry</Button></div>}
