import{CommunicationHeader,FollowUpList}from"@/features/leadestate/communication/components/CommunicationUI";import{FollowUpForm}from"@/features/leadestate/communication/components/CommunicationForms";import{FollowUpService}from"@/features/leadestate/communication/services/follow-up.service";export default async function Page({searchParams}:{searchParams:Promise<{range?:string}>}){const[{range="today"},items]=await Promise.all([searchParams,new FollowUpService().list()]);const now=new Date(),tomorrow=new Date(now.getTime()+864e5);const filtered=items.filter(x=>range==="completed"?x.status==="completed":range==="overdue"?x.status!=="completed"&&new Date(x.dueAt)<now:range==="tomorrow"?x.dueAt.startsWith(tomorrow.toISOString().slice(0,10)):range==="upcoming"?new Date(x.dueAt)>tomorrow:x.dueAt.startsWith(now.toISOString().slice(0,10)));return <main className="mx-auto max-w-5xl px-5 py-8"><CommunicationHeader title="Follow-ups" description="Keep every promise visible until the customer receives an answer."/><FollowUpForm/><nav className="mt-6 flex flex-wrap gap-2" aria-label="Follow-up range">{["today","tomorrow","overdue","upcoming","completed"].map(x=><a className={`rounded-full px-4 py-2 text-xs capitalize ${x===range?"bg-cyan-300 text-slate-950":"bg-white/5 text-slate-400"}`} href={`?range=${x}`} key={x}>{x}</a>)}</nav><FollowUpList items={filtered}/></main>}
