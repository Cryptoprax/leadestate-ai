import Link from"next/link";import{redirect}from"next/navigation";import{OrganizationService}from"@/features/onboarding/services/organization.service";import{WorkspaceService}from"@/features/onboarding/services/workspace.service";import{OperationsService}from"@/features/leadestate/operations/services/operations.service";import{OperationsDashboard}from"@/features/leadestate/operations/components/OperationsUI";const actions=[["Create property","/leadestate/properties/new","Add inventory"],["Create lead","/leadestate/leads/new","Capture an opportunity"],["Review tasks","/leadestate/tasks","Plan today"],["Invite team","/leadestate/team","Collaborate securely"]];export default async function Page({searchParams}:{searchParams:Promise<{welcome?:string}>}){const organization=await new OrganizationService().current();if(!organization)redirect("/onboarding");const[workspace,q,data]=await Promise.all([new WorkspaceService().first(),searchParams,new OperationsService().snapshot()]);const populated=data.tasks.length+data.meetings.length+data.visits.length+data.activity.length+data.hotLeads+Object.keys(data.pipeline).length>0;return <main className="mx-auto max-w-[96rem] px-5 py-8 sm:py-10"><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-cyan-300">{workspace?.name??organization.name}</p><h1 className="mt-3 text-3xl font-semibold tracking-tight sm:text-4xl">{q.welcome?"Your workspace is ready":"Good morning"}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-400">{populated?"Your team’s priorities, opportunities, and customer activity—ready for the day.":"Build momentum with your first customer, property, or teammate."}</p></div><div className="flex gap-2"><Link href="/leadestate/leads/new" className="focus-ring rounded-xl bg-cyan-300 px-4 py-2.5 text-sm font-semibold text-slate-950">New lead</Link><Link href="/leadestate/properties/new" className="focus-ring rounded-xl border border-white/10 px-4 py-2.5 text-sm text-slate-300 hover:bg-white/5">Add property</Link></div></div>{populated?<div className="mt-8"><OperationsDashboard data={data}/></div>:<section className="mt-8 overflow-hidden rounded-3xl border border-white/[.08] bg-gradient-to-br from-white/[.045] to-cyan-300/[.025] p-6 sm:p-8"><p className="text-xs uppercase tracking-widest text-slate-500">First-run guide</p><h2 className="mt-3 text-2xl font-semibold">Turn your first opportunity into momentum</h2><p className="mt-2 max-w-xl text-sm leading-6 text-slate-400">Choose a starting point. LeadEstate keeps the customer, property, communication, and next action connected.</p><div className="mt-7 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{actions.map(([label,href,meta])=><Link key={label} href={href} className="group focus-ring rounded-2xl border border-white/[.08] bg-black/10 p-5 hover:-translate-y-0.5 hover:border-cyan-300/30 hover:bg-cyan-300/[.035]"><span className="text-sm font-medium text-slate-200 group-hover:text-white">{label}</span><span className="mt-2 block text-xs text-slate-500">{meta} →</span></Link>)}</div></section>}</main>}
