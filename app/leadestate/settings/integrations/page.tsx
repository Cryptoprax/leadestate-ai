import{GoogleConnectCard}from"@/features/platform/integrations/google/components";import{WhatsAppConnectCard}from"@/features/platform/integrations/whatsapp/WhatsAppConnectCard";import{operationsContext}from"@/features/leadestate/operations/services/context";import{GoogleRepository}from"@/features/platform/integrations/google/repositories/google.repository";export default async function Page({searchParams}:{searchParams:Promise<{error?:string;success?:string}>}){const ctx=await operationsContext(),[credential,q,whatsapp]=await Promise.all([new GoogleRepository(ctx.client,ctx.organizationId,ctx.workspaceId).credential(),searchParams,ctx.client.from("whatsapp_connections").select("id").eq("workspace_id",ctx.workspaceId).eq("status","connected").is("deleted_at",null).maybeSingle()]);return <main className="mx-auto max-w-5xl px-5 py-8"><h1 className="text-3xl font-semibold">Production integrations</h1><p className="mt-2 text-slate-400">Secure connections for the tools your team uses every day.</p>{q.error&&<p role="alert" className="mt-4 text-red-300">{q.error}</p>}{q.success&&<p role="status" className="mt-4 text-emerald-300">{q.success}</p>}<div className="mt-8 grid gap-5 lg:grid-cols-2"><GoogleConnectCard connected={!!credential} email={credential?.email}/><WhatsAppConnectCard connected={!!whatsapp.data}/></div></main>}
