import{NotificationCenter}from"@/features/notifications/components/NotificationCenter";import{NotificationService}from"@/features/notifications/services/notification.service";export default async function Page(){const items=await new NotificationService().list();return <main className="mx-auto max-w-5xl px-5 py-8"><h1 className="text-3xl font-semibold">Notifications</h1><p className="mt-2 text-slate-400">Billing, communications, AI, and operational updates in one place.</p><NotificationCenter items={items}/></main>}
