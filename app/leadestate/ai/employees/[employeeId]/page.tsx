import{notFound}from"next/navigation";import{WorkspaceRenderer}from"@/features/leadestate/workspace-engine/components";import{AIService}from"@/features/leadestate/ai-workforce/services/ai.service";import{employeeWorkspaceModel}from"@/features/leadestate/ai-workforce/services/employee-workspace.service";export default async function Page({params,searchParams}:{params:Promise<{employeeId:string}>;searchParams:Promise<{tab?:string}>}){const[{employeeId},{tab="overview"},items]=await Promise.all([params,searchParams,new AIService().employees()]);const employee=items.find(x=>x.id===employeeId);if(!employee)notFound();return <WorkspaceRenderer model={employeeWorkspaceModel(employee)} activeTab={tab} editHref={`/leadestate/ai/employees/${employeeId}`}/>}
