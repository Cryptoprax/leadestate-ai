import{notFound}from"next/navigation";import type{Metadata}from"next";import{BlogArticlePage}from"@/features/marketing/components/Blog";import{MarketingService}from"@/features/marketing/services/marketing.service";export function generateStaticParams(){return new MarketingService().articles().map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const value=new MarketingService().article((await params).slug);return value?{title:value.article.title,description:value.article.excerpt,alternates:{canonical:`/blog/${value.article.slug}`},openGraph:{type:"article",title:value.article.title,description:value.article.excerpt}}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const value=new MarketingService().article((await params).slug);if(!value)notFound();return <BlogArticlePage {...value}/>}
