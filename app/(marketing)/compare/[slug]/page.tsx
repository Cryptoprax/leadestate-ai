import type{Metadata}from"next";import{notFound}from"next/navigation";import{ComparisonLanding}from"@/features/marketing/components/AssetPages";import{MarketingAssetsService}from"@/features/marketing/services/marketing-assets.service";const service=new MarketingAssetsService();export function generateStaticParams(){return service.catalog().comparisons.map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,page=service.comparison(slug);return page?{title:`VAYON vs ${page.competitor}`,description:page.summary,alternates:{canonical:`/compare/${slug}`},openGraph:{title:`VAYON vs ${page.competitor}`,description:page.summary,url:`/compare/${slug}`}}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const page=service.comparison((await params).slug);if(!page)notFound();return <ComparisonLanding page={page}/>}
