import type{Metadata}from"next";import{CatalogGrid}from"@/features/marketing/components/AssetPages";import{MarketingAssetsService}from"@/features/marketing/services/marketing-assets.service";export const metadata:Metadata={title:"Compare VAYON",description:"Transparent architectural comparisons for evaluating VAYON.",alternates:{canonical:"/compare"}};export default function Page(){const items=new MarketingAssetsService().catalog().comparisons.map(x=>({href:`/compare/${x.slug}`,title:`VAYON vs ${x.competitor}`,description:x.summary}));return <CatalogGrid eyebrow="Compare" title="Evaluate the operating model, not just the feature list." description="Transparent, qualification-aware comparisons designed to support responsible platform evaluation." items={items}/>}
