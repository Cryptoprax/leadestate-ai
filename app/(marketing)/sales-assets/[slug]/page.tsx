import type{Metadata}from"next";import{notFound}from"next/navigation";import{SalesAssetPage}from"@/features/marketing/components/AssetPages";import{MarketingAssetsService}from"@/features/marketing/services/marketing-assets.service";const service=new MarketingAssetsService();export function generateStaticParams(){return service.catalog().assets.map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,page=service.asset(slug);return page?{title:page.title,description:page.summary,alternates:{canonical:`/sales-assets/${slug}`}}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const page=service.asset((await params).slug);if(!page)notFound();return <SalesAssetPage asset={page}/>}
