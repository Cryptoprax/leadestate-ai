import type{Metadata}from"next";import{CatalogGrid}from"@/features/marketing/components/AssetPages";import{MarketingAssetsService}from"@/features/marketing/services/marketing-assets.service";export const metadata:Metadata={title:"Sales Assets",description:"Presentation-ready VAYON platform, workforce, workflow, security, and executive material.",alternates:{canonical:"/sales-assets"}};export default function Page(){const items=new MarketingAssetsService().catalog().assets.map(x=>({href:`/sales-assets/${x.slug}`,title:x.title,description:x.summary}));return <CatalogGrid eyebrow="Sales enablement" title="Clear assets for every buying conversation." description="Reusable product narratives for sales conversations, customer demos, and executive evaluation." items={items}/>}
