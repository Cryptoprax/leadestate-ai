import type{Metadata}from"next";import{notFound}from"next/navigation";import{DocumentationArticleView}from"@/features/platform/knowledge/components/DocumentationClient";import{DocumentationService}from"@/features/platform/knowledge/services/documentation.service";const service=new DocumentationService();
export function generateStaticParams(){return service.all().map(article=>({slug:article.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,item=service.article(slug);if(!item)return{};return{title:`${item.article.title} | Documentation`,description:item.article.description,alternates:{canonical:`/docs/${slug}`},openGraph:{title:item.article.title,description:item.article.description,url:`/docs/${slug}`,type:"article",modifiedTime:item.article.updatedAt}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,item=service.article(slug);if(!item)notFound();const jsonLd={"@context":"https://schema.org","@type":"TechArticle",headline:item.article.title,description:item.article.description,dateModified:item.article.updatedAt,version:item.article.version};return <main className="mx-auto max-w-[90rem] px-5 py-10 sm:px-8"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd).replaceAll("<","\\u003c")}}/><DocumentationArticleView article={item.article} related={item.related}/></main>}
