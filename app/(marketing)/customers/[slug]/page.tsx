import type{Metadata}from"next";import{notFound}from"next/navigation";import{CustomerStoryPage}from"@/features/marketing/components/AssetPages";import{MarketingAssetsService}from"@/features/marketing/services/marketing-assets.service";const service=new MarketingAssetsService();export function generateStaticParams(){return service.catalog().stories.map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,page=service.story(slug);return page?{title:page.title,description:page.summary,alternates:{canonical:`/customers/${slug}`}}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const page=service.story((await params).slug);if(!page)notFound();return <CustomerStoryPage story={page}/>}
