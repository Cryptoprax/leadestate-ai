import{AuthFields,AuthShell,FormNotice}from"@/features/authentication/components/AuthForm";import{resetPasswordAction}from"@/features/authentication/actions/auth.actions";export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <AuthShell title="Choose a new password" description="Use at least eight characters and keep it unique."><FormNotice {...q}/><form action={resetPasswordAction}><AuthFields kind="reset"/></form></AuthShell>}
