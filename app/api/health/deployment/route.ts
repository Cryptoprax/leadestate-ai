import{NextResponse}from"next/server";import{DeploymentService}from"@/features/platform/deployment/services/deployment.service";export const dynamic="force-dynamic";export async function GET(){try{const snapshot=await new DeploymentService().snapshot(),healthy=snapshot.health.every(x=>x.state==="healthy")&&snapshot.configuration.valid&&snapshot.migrations.current;return NextResponse.json(snapshot,{status:healthy?200:503,headers:{"Cache-Control":"no-store"}})}catch{return NextResponse.json({status:"unavailable",diagnostic:"Deployment health requires an authorized administrator."},{status:503,headers:{"Cache-Control":"no-store"}})}}
