import{GmailService}from"@/features/platform/integrations/google/services/gmail.service";import{integrationContext}from"@/features/platform/integrations/services/integration-context";
export async function GET(request:Request,{params}:{params:Promise<{messageId:string;attachmentId:string}>}){await integrationContext();const{messageId,attachmentId}=await params,{data}=await new GmailService().attachment(messageId,attachmentId),url=new URL(request.url),name=(url.searchParams.get("name")??"attachment").replace(/[\r\n"]/g,"");return new Response(Buffer.from(data,"base64url"),{headers:{"content-type":"application/octet-stream","content-disposition":`attachment; filename=\"${name}\"`,"cache-control":"private, no-store","x-content-type-options":"nosniff"}})}
