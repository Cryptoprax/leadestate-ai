import{NextResponse}from"next/server";import{WhatsAppService}from"@/features/platform/integrations/whatsapp/whatsapp.service";export async function GET(request:Request){const q=new URL(request.url).searchParams;if(q.get("hub.mode")==="subscribe"&&q.get("hub.verify_token")===process.env.WHATSAPP_VERIFY_TOKEN)return new NextResponse(q.get("hub.challenge")??"",{status:200});return new NextResponse("Forbidden",{status:403})}export async function POST(request:Request){const raw=await request.text(),signature=request.headers.get("x-hub-signature-256")??"",service=new WhatsAppService();if(!service.verifySignature(raw,signature))return NextResponse.json({error:"Invalid signature"},{status:401});try{await service.receive(JSON.parse(raw));return NextResponse.json({received:true})}catch{return NextResponse.json({error:"Processing failed"},{status:500})}}
