import {notFound} from "next/navigation";import {CreativeStudioService} from "@/features/vayon/creative-studio/service";import {StudioDashboard,StudioShell} from "@/features/vayon/creative-studio/components/StudioViews";export default async function Page(){const service=await CreativeStudioService.production();if(!service)notFound();return <StudioShell title="Marketing Studio" description="Build governed real estate campaign drafts from authoritative project, inventory, pricing and organization brand data."><StudioDashboard snapshot={await service.snapshot()}/></StudioShell>}
