import {notFound} from "next/navigation";import {CreativeStudioService} from "@/features/vayon/creative-studio/service";import {BrandKits,StudioShell} from "@/features/vayon/creative-studio/components/StudioViews";export default async function Page(){const service=await CreativeStudioService.production();if(!service)notFound();return <StudioShell title="Brand Kit Manager" description="Approved identity, contact, legal and tone settings automatically govern every generated draft."><BrandKits snapshot={await service.snapshot()}/></StudioShell>}
