import {notFound} from "next/navigation";import {CreativeStudioService} from "@/features/vayon/creative-studio/service";import {StudioShell} from "@/features/vayon/creative-studio/components/StudioViews";export default async function Page(){const service=await CreativeStudioService.production();if(!service)notFound();const{templates}=await service.snapshot();return <StudioShell title="Template Library" description="Premium, editable real estate campaign systems that inherit the selected Brand Kit."><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{templates.map(x=><article key={x.id} className="rounded-2xl border border-vds-border bg-vds-surface p-5"><p className="text-xs text-vds-primary">{x.category}</p><h2 className="mt-2 font-semibold">{x.name}</h2><p className="mt-3 text-sm text-vds-muted">Editable · Brand Kit aware · Draft only</p></article>)}</div></StudioShell>}
