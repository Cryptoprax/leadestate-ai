import {notFound} from "next/navigation";import {CreativeStudioService} from "@/features/vayon/creative-studio/service";import {CampaignWizard,CapabilityCatalog,StudioShell} from "@/features/vayon/creative-studio/components/StudioViews";export default async function Page(){const service=await CreativeStudioService.production();if(!service)notFound();const{inventory}=await service.projectContext();return <StudioShell title="Campaign Wizard" description="Five guided steps create a complete, editable, recommendation-only campaign package."><CampaignWizard projects={inventory.projects}/><div className="mt-6"><CapabilityCatalog/></div></StudioShell>}
