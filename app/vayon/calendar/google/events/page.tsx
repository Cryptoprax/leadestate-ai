import{GoogleCalendarEventDetail,GoogleCalendarWorkspace}from"@/features/platform/google-calendar/components";export default async function Page({searchParams}:{searchParams:Promise<{q?:string;view?:"day"|"week"|"month"|"agenda";page?:string;event?:string;calendar?:string}>}){const q=await searchParams;if(q.event&&q.calendar)return <GoogleCalendarEventDetail eventId={q.event} calendarId={q.calendar}/>;return <GoogleCalendarWorkspace section="events" query={q.q} view={q.view} pageToken={q.page}/>}
