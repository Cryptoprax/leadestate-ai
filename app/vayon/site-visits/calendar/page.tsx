import{VisitCalendar,VisitHeader}from"@/features/vayon/site-visits/VisitViews";import{SiteVisitLifecycleService}from"@/features/vayon/site-visits/service";
export default async function Page({searchParams}:{searchParams:Promise<{view?:string}>}){const{view="week"}=await searchParams,service=await SiteVisitLifecycleService.production(),snapshot=await service.snapshot();return <main className="mx-auto max-w-[100rem] px-4 py-8 sm:px-5"><VisitHeader title="Visit calendar" description="Plan day, week, month, and agenda schedules with working-hour, overlap, workload, and travel-buffer awareness."/><VisitCalendar snapshot={snapshot} view={view}/></main>}
