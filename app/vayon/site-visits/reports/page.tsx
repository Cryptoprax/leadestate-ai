import{VisitHeader,VisitReports}from"@/features/vayon/site-visits/VisitViews";import{SiteVisitLifecycleService}from"@/features/vayon/site-visits/service";
export default async function Page(){const service=await SiteVisitLifecycleService.production(),snapshot=await service.snapshot();return <main className="mx-auto max-w-[100rem] px-4 py-8 sm:px-5"><VisitHeader title="Visit reports" description="Review project, agent, source, conversion, no-show, duration, and peak-time performance from authoritative visit records."/><VisitReports snapshot={snapshot} metrics={service.metrics(snapshot)}/></main>}
