import{Button}from"@/features/platform/design-system";import{MicrosoftCapabilityShell}from"@/features/platform/integrations/microsoft/components/MicrosoftCapabilityShell";import{MicrosoftTeamsService}from"@/features/platform/integrations/microsoft/services/teams.service";export default async function Page({searchParams}:{searchParams:Promise<{chat?:string;q?:string}>}){const query=await searchParams,service=new MicrosoftTeamsService(),status=await service.status();let chats:null|Awaited<ReturnType<typeof service.chats>>=null,messages:null|Awaited<ReturnType<typeof service.messages>>=null,presence:null|Awaited<ReturnType<typeof service.presence>>=null,meetings:null|Awaited<ReturnType<typeof service.meetings>>=null;if(status==="connected")try{[chats,presence,meetings]=await Promise.all([service.chats(),service.presence(),service.meetings()]);if(query.chat)messages=await service.messages(query.chat,query.q)}catch{}return <MicrosoftCapabilityShell title="Microsoft Teams" capability="teams" status={status} description="Read-only collaboration workspace for chats, meetings, channels, files, presence, and search. Live messaging is not enabled."><div className="mt-6 grid gap-5 lg:grid-cols-[18rem_1fr_20rem]"><aside className="rounded-xl border border-vds-border bg-vds-surface p-4"><h2 className="font-semibold">Chats</h2><div className="mt-3 space-y-2">{chats?.map(chat=><a key={chat.id} href={`/vayon/communications/teams?chat=${chat.id}`} className="block rounded-lg border border-vds-border p-3 text-sm">{chat.topic??chat.chatType}</a>)}{!chats&&<p className="text-sm text-vds-muted">Teams data unavailable.</p>}</div><h2 className="mt-5 font-semibold">Channels</h2><p className="mt-2 text-xs text-vds-muted">Channel and file contracts are active when a team is selected by a future navigation surface.</p></aside><section><form className="flex gap-2"><input type="hidden" name="chat" value={query.chat??""}/><input name="q" defaultValue={query.q} placeholder="Search current chat" className="h-11 flex-1 rounded-xl border border-vds-border bg-vds-input px-3"/><Button>Search</Button></form><div className="mt-4 space-y-3">{messages?.map(message=><article key={message.id} className="rounded-xl border border-vds-border p-4"><strong>{message.sender}</strong><p className="mt-2 text-sm text-vds-muted">{message.bodyPreview}</p><p className="mt-2 text-xs text-vds-subtle">{message.createdAt?new Date(message.createdAt).toLocaleString():"Unknown time"}</p></article>)}{!query.chat&&<p className="rounded-xl border border-vds-border p-8 text-center text-vds-muted">Select a chat to inspect messages.</p>}</div></section><aside className="space-y-4"><section className="rounded-xl border border-vds-border bg-vds-surface p-4"><h2 className="font-semibold">Presence</h2><p className="mt-2 text-sm text-vds-muted">{presence?`${presence.availability} · ${presence.activity}`:"Unavailable"}</p></section><section className="rounded-xl border border-vds-border bg-vds-surface p-4"><h2 className="font-semibold">Meetings</h2>{meetings?.map(meeting=><article key={meeting.id} className="mt-3 text-sm"><strong>{meeting.subject}</strong><p className="text-vds-muted">{meeting.start?new Date(meeting.start).toLocaleString():"Time unavailable"}</p></article>)}</section><section className="rounded-xl border border-vds-border p-4"><h2 className="font-semibold">Messaging disabled</h2><p className="mt-2 text-xs text-vds-muted">No Teams message composer is active in this release. Workflow actions remain non-executable.</p></section></aside></div></MicrosoftCapabilityShell>}
