import{notFound}from"next/navigation";import{PropertyService}from"@/features/vayon/property/services/property.service";import{WorkspaceRenderer}from"@/features/vayon/workspace-engine/components/WorkspaceRenderer";import{normalizeWorkspaceTab,propertyWorkspaceModel}from"@/features/vayon/workspace-engine/services/workspace-adapter.service";import{WorkspaceRegistryService}from"@/features/vayon/workspace-engine/services/workspace-registry.service";export default async function Page({params,searchParams}:{params:Promise<{propertyId:string}>;searchParams:Promise<{tab?:string;success?:string}>}){const id=(await params).propertyId,property=await new PropertyService().detail(id);if(!property)notFound();const q=await searchParams,tabs=new WorkspaceRegistryService().visibleTabs("property-workspace"),active=normalizeWorkspaceTab(q.tab,tabs);return <>{q.success&&<p role="status" className="mx-auto mt-5 max-w-[96rem] rounded-xl bg-vds-success-soft p-3 text-sm text-vds-success">{q.success}</p>}<WorkspaceRenderer model={propertyWorkspaceModel(property)} activeTab={active} editHref={`/vayon/properties/${id}/edit`}/></>}
