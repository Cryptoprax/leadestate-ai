import{notFound}from"next/navigation";import{LeadService}from"@/features/vayon/lead/services/lead.service";import{WorkspaceRenderer}from"@/features/vayon/workspace-engine/components/WorkspaceRenderer";import{leadWorkspaceModel,normalizeWorkspaceTab}from"@/features/vayon/workspace-engine/services/workspace-adapter.service";import{WorkspaceRegistryService}from"@/features/vayon/workspace-engine/services/workspace-registry.service";export default async function Page({params,searchParams}:{params:Promise<{leadId:string}>;searchParams:Promise<{tab?:string;success?:string}>}){const id=(await params).leadId,lead=await new LeadService().detail(id);if(!lead)notFound();const q=await searchParams,tabs=new WorkspaceRegistryService().visibleTabs("lead-workspace"),active=normalizeWorkspaceTab(q.tab,tabs);return <>{q.success&&<p role="status" className="mx-auto mt-5 max-w-[96rem] rounded-xl bg-vds-success-soft p-3 text-sm text-vds-success">{q.success}</p>}<WorkspaceRenderer model={leadWorkspaceModel(lead)} activeTab={active} editHref={`/vayon/leads/${id}/edit`}/></>}
