import{notFound}from"next/navigation";import{updateLeadAction}from"@/features/vayon/lead/actions/lead.actions";import{LeadWizard}from"@/features/vayon/lead/components/LeadWizard";import{LeadService}from"@/features/vayon/lead/services/lead.service";export default async function Page({params,searchParams}:{params:Promise<{leadId:string}>;searchParams:Promise<{error?:string}>}){const id=(await params).leadId,lead=await new LeadService().detail(id);if(!lead)notFound();const q=await searchParams;return <main className="mx-auto max-w-4xl px-5 py-8"><h1 className="mb-7 text-3xl font-semibold">Edit lead</h1><LeadWizard lead={lead} error={q.error} action={updateLeadAction.bind(null,id)}/></main>}
