export function log(event:string,fields:Record<string,unknown>={}){const safe=Object.fromEntries(Object.entries(fields).filter(([key])=>!/secret|token|password|key/i.test(key)));console.info(JSON.stringify({level:"info",event,correlationId:safe.correlationId??crypto.randomUUID(),timestamp:new Date().toISOString(),...safe}))}export function captureException(error:unknown,context:Record<string,unknown>={}){const message=error instanceof Error?error.message:"Unknown error";console.error(JSON.stringify({level:"error",event:"exception",message,correlationId:context.correlationId??crypto.randomUUID(),timestamp:new Date().toISOString()}))}
