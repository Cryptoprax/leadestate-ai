"use client";
import { Button } from "@/features/platform/design-system";import{useRef,type ReactNode}from"react";export function ConfirmButton({children,title,description,confirmLabel="Confirm",destructive=false,onConfirm}:{children:ReactNode;title:string;description:string;confirmLabel?:string;destructive?:boolean;onConfirm:()=>void}){const ref=useRef<HTMLDialogElement>(null);return <><span onClick={()=>ref.current?.showModal()}>{children}</span><dialog ref={ref} aria-labelledby="confirm-title" className="m-auto w-[calc(100%-2rem)] max-w-md rounded-3xl border border-vds-border bg-[var(--vds-color-surface)] p-0 text-vds-foreground shadow-2xl backdrop:bg-vds-overlay"><div className="p-6"><h2 id="confirm-title" className="text-xl font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-vds-muted">{description}</p><div className="mt-6 flex justify-end gap-2"><Button variant="ghost" onClick={()=>ref.current?.close()}>Cancel</Button><Button variant={destructive?"danger":"primary"} onClick={()=>{onConfirm();ref.current?.close()}}>{confirmLabel}</Button></div></div></dialog></>}
