import"server-only";import{operationsContext}from"@/features/vayon/operations/services/context";import{CommunicationRepository}from"../repositories/communication.repository";export class CommunicationService{private async repo(){const c=await operationsContext();return new CommunicationRepository(c.client,c.organizationId,c.workspaceId)}async threads(){return(await this.repo()).threads()}async timeline(){return(await this.repo()).timeline()}async addNote(input:{threadId:string;body:string}){return(await this.repo()).addNote(input)}async archive(id:string,version:number){return(await this.repo()).archive(id,version)}}
