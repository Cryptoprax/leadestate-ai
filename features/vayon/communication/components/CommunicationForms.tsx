import { Button } from "@/features/platform/design-system";
import{addCommunicationNoteAction,createCallAction,createFollowUpAction}from"../actions/communication.actions";const field="rounded-xl border border-vds-border bg-vds-input px-4 py-3 text-sm";const form="grid gap-3 rounded-3xl border border-vds-border/[.08] bg-vds-surface/[.03] p-5 md:grid-cols-2";export function CallForm(){return <form action={createCallAction} className={form}><input className={field} name="durationSeconds" type="number" min="0" placeholder="Duration in seconds" required/><input className={field} name="outcome" placeholder="Call outcome" required/><input className={field} name="leadId" placeholder="Lead UUID (optional)"/><input className={field} name="dealId" placeholder="Deal UUID (optional)"/><input className={field} name="nextFollowUpAt" type="datetime-local"/><Button variant="primary" className="rounded-xl px-5 py-3 font-semibold">Log call</Button></form>}export function FollowUpForm(){return <form action={createFollowUpAction} className={form}><input className={field} name="title" placeholder="Follow-up commitment" required/><input className={field} name="dueAt" type="datetime-local" required/><select className={field} name="priority"><option>low</option><option>medium</option><option>high</option><option>urgent</option></select><Button variant="primary" className="rounded-xl px-5 py-3 font-semibold">Create follow-up</Button></form>}export function NoteForm(){return <form action={addCommunicationNoteAction} className={form}><input className={field} name="threadId" placeholder="Conversation UUID" required/><textarea className={field} name="body" placeholder="Internal note" required/><Button variant="primary" className="rounded-xl px-5 py-3 font-semibold">Add note</Button></form>}
