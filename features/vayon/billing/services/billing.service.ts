import"server-only";import{BillingRepository}from"../repositories/billing.repository";import{billingContext}from"./billing-context";import{SubscriptionService}from"./subscription.service";import{UsageService}from"./usage.service";import{InvoiceService}from"./invoice.service";import type{BillingDashboard}from"../types";export class BillingService{private async repo(){const c=await billingContext();return new BillingRepository(c.client,c.organizationId,c.workspaceId)}async contact(){return(await this.repo()).contact()}async updateContact(input:{companyName:string;billingEmail:string;taxId?:string}){return(await this.repo()).updateContact(input)}async dashboard():Promise<BillingDashboard>{await billingContext();const[plans,subscription,usage,invoices,contact]=await Promise.all([new SubscriptionService().plans(),new SubscriptionService().current(),new UsageService().list(),new InvoiceService().list(),this.contact()]);return{plans,subscription,usage,invoices,contact}}}
