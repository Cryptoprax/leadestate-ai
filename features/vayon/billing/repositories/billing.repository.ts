import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";import type{BillingContactRecord}from"../types";export class BillingRepository{constructor(private c:SupabaseClient,private o:string,private w:string){}async contact(){const{data,error}=await this.c.from("billing_contacts").select("id,company_name,billing_email,tax_id,address,version").eq("organization_id",this.o).eq("workspace_id",this.w).maybeSingle();if(error)throw error;return data?{id:data.id,companyName:data.company_name,billingEmail:data.billing_email,taxId:data.tax_id??undefined,address:data.address??undefined,version:data.version}as BillingContactRecord:null}async updateContact(input:unknown){const{error}=await this.c.rpc("update_billing_contact",{p_workspace_id:this.w,p_input:input});if(error)throw error}}
