import { Button } from "@/features/platform/design-system";
import{cancelSubscriptionAction,generateInvoicePlaceholderAction,updateBillingContactAction}from"../actions/billing.actions";import type{BillingContactRecord,SubscriptionRecord}from"../types";const field="rounded-xl border border-vds-border bg-vds-input px-4 py-3 text-sm";export function BillingContactForm({contact}:{contact:BillingContactRecord|null}){return <form action={updateBillingContactAction} className="grid gap-3 rounded-3xl border border-vds-border/[.08] bg-vds-surface/[.03] p-5"><input className={field} name="companyName" defaultValue={contact?.companyName} placeholder="Legal company name" required/><input className={field} name="billingEmail" type="email" defaultValue={contact?.billingEmail} placeholder="Billing email" required/><input className={field} name="taxId" defaultValue={contact?.taxId} placeholder="GST / Tax ID (placeholder)"/><div className="rounded-xl border border-dashed border-vds-border p-4 text-sm text-vds-muted">Payment method placeholder — available when a provider is connected.</div><Button variant="primary" className="rounded-xl px-5 py-3 font-semibold">Save billing details</Button></form>}export function CancelSubscription({subscription}:{subscription:SubscriptionRecord|null}){if(!subscription)return null;return <form action={cancelSubscriptionAction}><input type="hidden" name="expectedVersion" value={subscription.version}/><Button variant="control" className="rounded-xl border border-vds-danger px-4 py-3 text-sm text-vds-danger">Cancel at period end</Button></form>}export function GenerateDraftInvoice(){return <form action={generateInvoicePlaceholderAction}><Button variant="control" className="rounded-xl border border-vds-border px-4 py-3 text-sm text-vds-secondary">Generate draft invoice</Button></form>}
