import type{AIOutput}from"../validation/output";export interface AIGenerationRequest{prompt:string;system:string;cacheKey:string;metadata:{organizationId:string;workspaceId:string;employeeCode:string;promptKey:string;subjectType?:string;subjectId?:string}}export interface AIGenerationResult{output:AIOutput;provider:string;model:string;latencyMs:number;inputTokens:number;outputTokens:number;costEstimate:number}export interface AIProvider{readonly id:string;generateText(request:AIGenerationRequest):Promise<AIGenerationResult>;generateStructured(request:AIGenerationRequest):Promise<AIGenerationResult>;embed():Promise<never>;stream():Promise<never>}
