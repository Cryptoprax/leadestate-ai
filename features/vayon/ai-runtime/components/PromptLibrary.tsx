import Link from "next/link";
import { promptRegistry } from "../prompts/registry";

const card = "rounded-2xl border border-vds-border bg-vds-surface p-5";

export function PromptLibrary() {
  return <section className="mt-7 space-y-5"><div className="grid gap-4 md:grid-cols-2">{Object.values(promptRegistry).map((prompt) => <article className={card} key={prompt.key}><div className="flex items-start justify-between gap-3"><div><p className="text-xs uppercase tracking-[.16em] text-vds-primary">Role prompt · v{prompt.version}</p><h2 className="mt-2 font-semibold">{prompt.title}</h2></div><span className="rounded-full bg-vds-elevated px-2 py-1 text-[10px]">Active</span></div><p className="mt-4 line-clamp-3 text-sm leading-6 text-vds-muted">{prompt.instruction}</p><div className="mt-4 flex flex-wrap gap-1.5">{prompt.employeeCodes.map((code) => <span className="rounded-full border border-vds-border px-2 py-1 text-[10px]" key={code}>{code.replaceAll("_", " ")}</span>)}</div><Link href="/vayon/ai/workforce" className="mt-5 inline-block text-sm text-vds-primary">Test through governed employee chat</Link></article>)}</div><article className={card}><h2 className="font-semibold">Prompt governance</h2><dl className="mt-4 grid gap-4 sm:grid-cols-4"><div><dt className="text-xs text-vds-muted">Version history</dt><dd className="mt-1 text-sm">Immutable version identifiers</dd></div><div><dt className="text-xs text-vds-muted">System prompts</dt><dd className="mt-1 text-sm">Runtime-owned safety policy</dd></div><div><dt className="text-xs text-vds-muted">Templates</dt><dd className="mt-1 text-sm">Role-scoped registry</dd></div><div><dt className="text-xs text-vds-muted">Testing</dt><dd className="mt-1 text-sm">Live employee runtime only</dd></div></dl><p className="mt-4 text-xs text-vds-muted">Direct ungoverned execution remains disabled. Tests inherit workspace attribution, observability, and approval policy.</p></article></section>;
}
