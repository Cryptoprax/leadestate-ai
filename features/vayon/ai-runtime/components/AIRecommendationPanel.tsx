import { Button } from "@/features/platform/design-system";
import{generateAIRecommendationAction}from"../actions/recommendation.actions";import{promptRegistry}from"../prompts/registry";export function AIRecommendationPanel(){return <section className="mb-6 rounded-2xl border border-vds-accent-border bg-vds-primary/[.04] p-5"><p className="text-xs font-semibold uppercase tracking-[.2em] text-vds-primary">Live AI runtime</p><h2 className="mt-2 text-xl font-semibold">Generate a governed recommendation</h2><p className="mt-1 text-sm text-vds-muted">OpenAI produces read-only structured output. Every action remains subject to human approval.</p><form action={generateAIRecommendationAction} className="mt-4 grid gap-3 md:grid-cols-[.7fr_1.3fr_auto]"><select name="promptKey" aria-label="AI capability" className="rounded-xl border border-vds-border bg-[var(--vds-color-surface)] px-3 py-2">{Object.values(promptRegistry).map(p=><option key={p.key} value={p.key}>{p.title}</option>)}</select><input name="instruction" placeholder="Optional focus or instruction" aria-label="Optional instruction" className="rounded-xl border border-vds-border bg-vds-input px-3 py-2"/><Button variant="primary" className="rounded-xl px-4 py-2 font-semibold">Generate</Button></form></section>}
