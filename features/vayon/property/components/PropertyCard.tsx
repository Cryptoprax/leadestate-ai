import Link from"next/link";import type{PropertyRecord}from"../types";import{PropertyStatusBadge}from"./PropertyStatusBadge";const money=(p:PropertyRecord)=>{const m=p.salePrice??p.rentalPrice;return m?new Intl.NumberFormat(undefined,{style:"currency",currency:m.currency,maximumFractionDigits:0}).format(m.amount):"Price on request"};export function PropertyCard({property}:{property:PropertyRecord}){return <article className="overflow-hidden rounded-3xl border border-vds-border/[0.08] bg-[var(--vds-color-surface)]"><div className="grid h-44 place-items-center bg-gradient-to-br from-vds-elevated to-vds-background text-xs uppercase tracking-widest text-vds-subtle">Property media</div><div className="p-5"><div className="flex justify-between gap-3"><PropertyStatusBadge status={property.status}/><span className="text-xs text-vds-subtle">{property.reference}</span></div><h2 className="mt-4 text-lg font-semibold">{property.title}</h2><p className="mt-1 text-sm text-vds-muted">{property.propertyType} · {property.address.city}</p><p className="mt-4 font-semibold text-vds-primary">{money(property)}</p><div className="mt-5 flex justify-between border-t border-vds-border/[0.06] pt-4 text-xs text-vds-muted"><span>Agent: {property.assignedAgentName??"Unassigned"}</span><Link href={`/vayon/properties/${property.id}`} className="text-vds-primary">Open →</Link></div></div></article>}
