import"server-only";import{createSupabaseServerClient}from"@/lib/supabase/server";import{OrganizationService}from"@/features/onboarding/services/organization.service";import{WorkspaceService}from"@/features/onboarding/services/workspace.service";export async function operationsContext(){const[organization,workspace,client]=await Promise.all([new OrganizationService().current(),new WorkspaceService().first(),createSupabaseServerClient()]);if(!organization||!workspace)throw new Error("Active organization and workspace required.");return{organizationId:organization.id,workspaceId:workspace.id,client}}
