import{leadPriorities,leadStatuses}from"../config/catalogs";function badge(value:string,label:string,tone:string){return <span className={`rounded-full border px-2.5 py-1 text-[10px] font-semibold uppercase tracking-wide ${tone}`}>{label}</span>}export function LeadStatusBadge({status}:{status:string}){return badge(status,leadStatuses.find(x=>x.code===status)?.label.default??status,"border-vds-accent-border bg-vds-primary/[0.07] text-vds-primary")}export function LeadPriorityBadge({priority}:{priority:string}){return badge(priority,leadPriorities.find(x=>x.code===priority)?.label.default??priority,priority==="urgent"||priority==="vip"?"border-vds-danger bg-vds-danger-soft text-vds-danger":"border-vds-border bg-vds-elevated text-vds-muted")}
