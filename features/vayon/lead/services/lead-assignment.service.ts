import{LeadService}from"./lead.service";export class LeadAssignmentService extends LeadService{async assign(ids:readonly string[],agentId:string){if(!await this.canManage())throw new Error("You do not have permission to assign leads.");return(await this.context()).repository.assign(ids,agentId)}async changeStatus(ids:readonly string[],status:string){return(await this.context()).repository.changeStatus(ids,status)}}
