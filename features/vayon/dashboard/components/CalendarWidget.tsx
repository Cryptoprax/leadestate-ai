import Link from "next/link";
import { CalendarDays, CloudSun, Map, MapPin, Phone, Route, SquareCheckBig } from "lucide-react";
import type { CalendarItem } from "../types";
const icons={meeting:CalendarDays,visit:MapPin,call:Phone,task:SquareCheckBig};
export function CalendarWidget({items}:{items:CalendarItem[]}){return <section className="rounded-3xl border border-vds-border/[0.075] bg-vds-surface/[0.025] p-5 sm:p-6" aria-labelledby="calendar-heading"><div className="flex items-center justify-between"><div><p className="text-xs font-medium uppercase tracking-[.18em] text-vds-primary">Today</p><h2 id="calendar-heading" className="mt-2 text-xl font-semibold">Calendar</h2></div><Link href="/vayon/calendar" className="text-xs text-vds-muted hover:text-vds-primary">Full calendar →</Link></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl border border-vds-border/[0.06] bg-vds-input p-4"><div className="flex items-center gap-2 text-xs text-vds-muted"><CloudSun className="size-4 text-vds-warning"/>Weather</div><p className="mt-3 text-sm text-vds-muted">Connect a weather provider</p><p className="mt-1 text-xs text-vds-subtle">Forecast placeholder</p></div><div className="rounded-2xl border border-vds-border/[0.06] bg-[radial-gradient(circle_at_30%_40%,var(--vds-color-primary-soft),transparent_35%)] p-4"><div className="flex items-center gap-2 text-xs text-vds-muted"><Map className="size-4 text-vds-primary"/>Route map</div><p className="mt-3 text-sm text-vds-muted">No map provider connected</p><p className="mt-1 text-xs text-vds-subtle">Map placeholder</p></div></div><div className="mt-5 divide-y divide-vds-divider/[0.06]">{items.slice(0,6).map((item)=>{const Icon=icons[item.kind];return <Link href={item.href} key={`${item.kind}-${item.id}`} className="focus-ring flex items-center gap-3 rounded-xl py-3 hover:px-2 hover:bg-vds-surface/[0.025]"><time className="w-16 shrink-0 text-xs font-medium text-vds-primary">{new Date(item.startsAt).toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"})}</time><span className="grid size-8 shrink-0 place-items-center rounded-xl bg-vds-surface/[0.05] text-vds-secondary"><Icon className="size-4"/></span><span className="min-w-0 flex-1"><span className="block truncate text-sm text-vds-secondary">{item.title}</span><span className="block truncate text-xs capitalize text-vds-subtle">{item.meta}</span></span></Link>})}{!items.length&&<div className="py-10 text-center"><Route className="mx-auto size-6 text-vds-subtle"/><p className="mt-3 text-sm text-vds-muted">Your day is open.</p></div>}</div></section>}
