import { Cable, CircleDashed } from "lucide-react";
import { AWAITING_BUSINESS_DATA } from "../services/narrative-engine";
export function AwaitingData({ detail = "Connect an approved business source to activate this view.", compact = false }: { readonly detail?: string; readonly compact?: boolean }) { return <div className={`rounded-2xl border border-dashed border-vds-border bg-vds-input text-center ${compact ? "p-4" : "px-5 py-7"}`}><span className="mx-auto grid size-9 place-items-center rounded-xl bg-vds-primary/[.06] text-vds-primary">{compact ? <CircleDashed className="size-4" aria-hidden="true"/> : <Cable className="size-4" aria-hidden="true"/>}</span><p className="mt-3 text-sm font-medium text-vds-secondary">{AWAITING_BUSINESS_DATA}</p><p className="mx-auto mt-1 max-w-xs text-xs leading-5 text-vds-subtle">{detail}</p></div> }
