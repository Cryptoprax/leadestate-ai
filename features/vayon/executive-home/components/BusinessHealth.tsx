import { HeartPulse } from "lucide-react";
import type { BusinessHealthModel } from "../domain/contracts";
import { ExecutiveCard } from "./ExecutiveCard";
export function BusinessHealth({ model }: { readonly model: BusinessHealthModel }) { const ready=model.calculationStatus==="ready"&&model.score!==undefined;return <ExecutiveCard title="Business Health" eyebrow={ready?"Deterministic readiness":"Future composite score"} icon={<HeartPulse className="size-4" aria-hidden="true"/>}><div className="flex items-center gap-5"><div className="grid size-24 shrink-0 place-items-center rounded-full border border-dashed border-vds-accent-border bg-vds-primary/[.035]"><div className="text-center"><p className="text-2xl font-semibold text-vds-muted">{ready?model.score:"—"}</p><p className="text-[9px] uppercase tracking-[.14em] text-vds-subtle">{ready?"Readiness":"No score"}</p></div></div><div><p className="text-sm font-medium text-vds-secondary">{model.explanation}</p><p className="mt-2 text-xs leading-5 text-vds-subtle">{ready?`Confidence ${Math.round((model.confidence??0)*100)}% · transparent coverage rules.`:"Calculation and confidence remain unavailable until governed source data and scoring policy are configured."}</p></div></div></ExecutiveCard> }
