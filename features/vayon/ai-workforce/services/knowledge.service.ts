import"server-only";import{operationsContext}from"@/features/vayon/operations/services/context";import{KnowledgeRepository}from"../repositories/knowledge.repository";import type{z}from"zod";import type{knowledgeSchema}from"../validation";export class KnowledgeService{private async repo(){const c=await operationsContext();return new KnowledgeRepository(c.client,c.organizationId,c.workspaceId)}async list(){return(await this.repo()).list()}async create(input:z.infer<typeof knowledgeSchema>){return(await this.repo()).create(input)}}
