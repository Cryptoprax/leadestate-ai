import"server-only";import{operationsContext}from"@/features/vayon/operations/services/context";import{RecommendationRepository}from"../repositories/recommendation.repository";export class RecommendationService{private async repo(){const c=await operationsContext();return new RecommendationRepository(c.client,c.organizationId,c.workspaceId)}async list(){return(await this.repo()).list()}async archive(id:string,version:number){return(await this.repo()).archive(id,version)}}
