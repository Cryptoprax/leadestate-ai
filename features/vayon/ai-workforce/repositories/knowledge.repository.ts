import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";import type{KnowledgeRecord}from"../types";export class KnowledgeRepository{constructor(private c:SupabaseClient,private o:string,private w:string){}async list(){const{data,error}=await this.c.from("ai_knowledge").select("id,source_type,title,description,status,created_at,version").eq("organization_id",this.o).eq("workspace_id",this.w).is("deleted_at",null).order("created_at",{ascending:false});if(error)throw error;return(data??[]).map(r=>({id:r.id,sourceType:r.source_type,title:r.title,description:r.description??undefined,status:r.status,createdAt:r.created_at,version:r.version}))as KnowledgeRecord[]}async create(input:unknown){const{data,error}=await this.c.rpc("create_ai_knowledge",{p_workspace_id:this.w,p_input:input});if(error)throw error;return String(data)}}
