import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";import type{RecommendationRecord}from"../types";export class RecommendationRepository{constructor(private c:SupabaseClient,private o:string,private w:string){}async list(){const{data,error}=await this.c.from("ai_recommendations").select("id,employee_id,recommendation_type,title,summary,confidence,status,subject_type,subject_id,created_at,version").eq("organization_id",this.o).eq("workspace_id",this.w).is("deleted_at",null).order("created_at",{ascending:false});if(error)throw error;return(data??[]).map(r=>({id:r.id,employeeId:r.employee_id,recommendationType:r.recommendation_type,title:r.title,summary:r.summary,confidence:r.confidence??undefined,status:r.status,subjectType:r.subject_type??undefined,subjectId:r.subject_id??undefined,createdAt:r.created_at,version:r.version}))as RecommendationRecord[]}async archive(id:string,version:number){const{error}=await this.c.rpc("archive_ai_recommendation",{p_recommendation_id:id,p_expected_version:version});if(error)throw error}}
