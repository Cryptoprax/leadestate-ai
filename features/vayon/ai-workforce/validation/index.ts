import{z}from"zod";export const knowledgeSchema=z.object({sourceType:z.enum(["properties","leads","deals","operations","communications","documents","policies","faqs"]),title:z.string().trim().min(2).max(160),description:z.string().trim().max(4000).optional()});export const decisionSchema=z.object({approvalId:z.string().uuid(),version:z.coerce.number().int().positive(),reason:z.string().trim().max(1000).optional()});export const archiveSchema=z.object({recommendationId:z.string().uuid(),version:z.coerce.number().int().positive()});
