import { Building2,FileText,UsersRound } from "lucide-react";import { auroraRealtyGroup } from "../config/aurora-realty-group";import { auroraEmployees } from "../people/employees";import { auroraDocuments } from "../sales-operations/records";
export function ExecutiveWorkspacePanel(){const departments=auroraRealtyGroup.departments.map(item=>`${item.name} · ${auroraEmployees.filter(employee=>employee.departmentId===item.id).length} connected employees`),documents=auroraDocuments.slice(-6).map(item=>`${item.title} · ${item.status}`);return <section className="grid gap-5 md:grid-cols-3"><article className="rounded-3xl border border-vds-border bg-vds-surface p-5"><Building2 className="size-5 text-vds-primary"/><h2 className="mt-3 font-semibold">Workspace overview</h2><p className="mt-2 text-sm text-vds-muted">{auroraRealtyGroup.company.name} · {auroraRealtyGroup.offices.length} offices · local demo workspace</p></article><article className="rounded-3xl border border-vds-border bg-vds-surface p-5"><UsersRound className="size-5 text-vds-primary"/><h2 className="mt-3 font-semibold">Business units and department activity</h2><p className="mt-2 text-xs text-vds-muted">{auroraRealtyGroup.businessUnits.map(item=>item.name).join(" · ")}</p><ul className="mt-3 space-y-1">{departments.slice(0,5).map(item=><li key={item} className="text-[10px] text-vds-subtle">{item}</li>)}</ul></article><article className="rounded-3xl border border-vds-border bg-vds-surface p-5"><FileText className="size-5 text-vds-primary"/><h2 className="mt-3 font-semibold">Recent documents</h2><ul className="mt-3 space-y-1">{documents.map(item=><li key={item} className="truncate text-[10px] text-vds-subtle">{item}</li>)}</ul></article></section>}
