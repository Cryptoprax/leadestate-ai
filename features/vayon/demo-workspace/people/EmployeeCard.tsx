import Image from "next/image";
import { Mail, MapPin, Phone, UsersRound } from "lucide-react";
import { auroraRealtyGroup } from "../config/aurora-realty-group";
import type { AuroraEmployee } from "./contracts";
export function EmployeeCard({employee,manager,directReports=[]}:{readonly employee:AuroraEmployee;readonly manager?:AuroraEmployee;readonly directReports?:readonly AuroraEmployee[]}){const department=auroraRealtyGroup.departments.find(item=>item.id===employee.departmentId)?.name??employee.departmentId,office=auroraRealtyGroup.offices.find(item=>item.id===employee.officeId)?.name??employee.officeId;return <article className="rounded-3xl border border-vds-border bg-vds-surface p-5 shadow-sm shadow-vds-shadow"><div className="flex items-start gap-4"><Image src={employee.avatarPlaceholder} alt="" width={52} height={52} className="size-13 rounded-2xl border border-vds-border"/><div className="min-w-0"><h3 className="truncate font-semibold">{employee.name}</h3><p className="mt-1 text-sm text-vds-primary">{employee.title}</p><p className="mt-1 text-xs text-vds-muted">{department} · {office}</p></div></div><p className="mt-4 text-sm leading-6 text-vds-muted">{employee.biography}</p>{employee.businessUnitIds.length>0&&<div className="mt-4 flex flex-wrap gap-2">{employee.businessUnitIds.map(unit=><span key={unit} className="rounded-full bg-vds-primary-soft px-2.5 py-1 text-[10px] capitalize text-vds-primary">{unit}</span>)}</div>}<dl className="mt-4 grid gap-2 text-xs text-vds-muted"><div className="flex items-center gap-2"><Mail className="size-4"/><dd className="truncate">{employee.email}</dd></div><div className="flex items-center gap-2"><Phone className="size-4"/><dd>{employee.phone}</dd></div><div className="flex items-center gap-2"><MapPin className="size-4"/><dd>{office}</dd></div><div className="flex items-center gap-2"><UsersRound className="size-4"/><dd>{manager?`Reports to ${manager.name}`:"Top-level executive"} · {directReports.length} direct report{directReports.length===1?"":"s"}</dd></div></dl><div className="mt-4 flex flex-wrap gap-2">{employee.skills.map(skill=><span key={skill} className="rounded-full border border-vds-border px-2.5 py-1 text-[10px] text-vds-subtle">{skill}</span>)}</div></article>}
