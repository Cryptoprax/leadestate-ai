"use client";
import { useMemo } from "react";import { locationData } from "../services/location-data.service";import { RegionalSelect } from "./RegionalSelect";
export function PhoneCodeSelect({name="phoneCode",label="Phone country code",countryCode,value,defaultValue,onChange,required}:{name?:string;label?:string;countryCode?:string;value?:string;defaultValue?:string;onChange?:(value:string)=>void;required?:boolean}){const options=useMemo(()=>locationData.countries().map(c=>({value:c.phoneCode,label:`${c.flag} ${c.phoneCode}`,detail:c.name})),[]);return <RegionalSelect name={name} label={label} value={value} defaultValue={defaultValue??locationData.country(countryCode??"")?.phoneCode} onChange={onChange} options={options} placeholder="Select phone code" required={required}/>}
