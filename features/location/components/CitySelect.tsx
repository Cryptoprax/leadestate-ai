"use client";
import { useMemo } from "react";import { locationData } from "../services/location-data.service";import { RegionalSelect } from "./RegionalSelect";
export function CitySelect({countryCode,state,name="city",label="City",value,defaultValue,onChange,required}:{countryCode:string;state:string;name?:string;label?:string;value?:string;defaultValue?:string;onChange?:(value:string)=>void;required?:boolean}){const stateCode=locationData.states(countryCode).find(item=>item.name===state||item.code===state)?.code??state;const options=useMemo(()=>locationData.cities(countryCode,stateCode).map(c=>({value:c.name,label:c.name})),[countryCode,stateCode]);return <RegionalSelect name={name} label={label} value={value} defaultValue={defaultValue} onChange={onChange} options={options} placeholder={options.length?"Select city":"No configured cities"} required={required} disabled={!countryCode||!state}/>}
