import{MarketingAssetsRepository}from"../repositories/marketing-assets.repository";export class MarketingAssetsService{constructor(private repository=new MarketingAssetsRepository()){}catalog(){return{industries:this.repository.industries(),comparisons:this.repository.comparisons(),assets:this.repository.assets(),stories:this.repository.stories()}}industry(slug:string){return this.repository.industry(slug)}comparison(slug:string){return this.repository.comparison(slug)}asset(slug:string){return this.repository.asset(slug)}story(slug:string){return this.repository.story(slug)}}
