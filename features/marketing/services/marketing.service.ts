import{MarketingRepository}from"../repositories/marketing.repository";import type{PublicPageId}from"../contracts";export class MarketingService{constructor(private repository=new MarketingRepository()){}page(id:PublicPageId){return this.repository.page(id)}articles(query=""){return this.repository.articles(query)}article(slug:string){const article=this.repository.article(slug);return article?{article,related:this.repository.related(article)}:null}}
