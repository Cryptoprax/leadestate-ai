import Link from"next/link";import type{BlogArticle}from"../contracts";export function BlogIndex({articles,query}:{articles:readonly BlogArticle[];query:string}){return <main className="mx-auto max-w-[90rem] px-5 py-24 sm:px-8"><p className="text-xs font-semibold uppercase tracking-[.2em] text-vds-primary">Vayon Journal</p><h1 className="mt-3 text-5xl font-semibold">Ideas for intelligent real estate operations.</h1><form className="mt-8"><input aria-label="Search articles" className="h-12 w-full max-w-xl rounded-xl border border-vds-border bg-vds-input px-4" name="q" defaultValue={query} placeholder="Search categories, authors, tags, and articles"/></form><div className="mt-10 grid gap-4 md:grid-cols-3">{articles.map(x=><article className="rounded-2xl border border-vds-border bg-vds-surface p-6" key={x.slug}><p className="text-xs text-vds-primary">{x.category} · {x.author}</p><h2 className="mt-3 text-xl font-semibold"><Link href={`/blog/${x.slug}`}>{x.title}</Link></h2><p className="mt-3 text-sm text-vds-muted">{x.excerpt}</p><p className="mt-4 text-xs text-vds-subtle">{x.tags.join(" · ")}</p></article>)}</div>{!articles.length&&<p className="mt-10 text-vds-muted">No articles match this search.</p>}</main>}export function BlogArticlePage({article,related}:{article:BlogArticle;related:readonly BlogArticle[]}){return <main className="mx-auto max-w-4xl px-5 py-24 sm:px-8"><p className="text-xs text-vds-primary">{article.category} · {article.author} · {article.publishedAt}</p><h1 className="mt-4 text-5xl font-semibold">{article.title}</h1><div className="mt-10 space-y-6 text-lg leading-8 text-vds-muted">{article.body.map(x=><p key={x}>{x}</p>)}</div><section className="mt-16 border-t border-vds-border pt-8"><h2 className="text-2xl font-semibold">Related articles</h2><div className="mt-5 grid gap-3 md:grid-cols-3">{related.map(x=><Link className="rounded-xl border border-vds-border p-4" href={`/blog/${x.slug}`} key={x.slug}>{x.title}</Link>)}</div></section></main>}
