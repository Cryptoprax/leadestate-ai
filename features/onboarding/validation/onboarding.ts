import{z}from"zod";import{emailSchema}from"@/features/authentication/validation/auth";export const roleCodes=["organization_owner","organization_admin","branch_manager","sales_manager","agent","viewer"]as const;export const onboardingSchema=z.object({organizationName:z.string().trim().min(2).max(120),country:z.string().trim().length(2),currency:z.string().trim().length(3),timezone:z.string().trim().min(3).max(100),language:z.string().trim().min(2).max(10),workspaceName:z.string().trim().min(2).max(120),office:z.string().trim().max(120).optional(),branch:z.string().trim().max(120).optional(),invitations:z.array(z.object({name:z.string().trim().min(2).max(100),email:emailSchema,role:z.enum(roleCodes)})).max(20)});export type OnboardingInput=z.infer<typeof onboardingSchema>;
