import"server-only";import type{Workspace}from"@/features/platform/workspaces/types/workspace";import{createSupabaseServerClient}from"@/lib/supabase/server";type WorkspaceProjection={workspaces:{id:string;name:string}|null};export class WorkspaceService{async first():Promise<Pick<Workspace,"id"|"name">|null>{const client=await createSupabaseServerClient();const{data}=await client.from("workspace_members").select("workspaces(id,name)").eq("status","active").limit(1).maybeSingle();const workspace=(data as unknown as WorkspaceProjection|null)?.workspaces;if(!workspace)return null;return{id:workspace.id,name:workspace.name}}}
