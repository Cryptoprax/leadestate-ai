import Link from "next/link";
import { ArrowUpRight, TrendingDown, TrendingUp } from "lucide-react";
import type { PipelineColumn } from "../types";

export function PipelineBoard({items,currency}:{items:PipelineColumn[];currency:string}){const format=new Intl.NumberFormat("en",{style:"currency",currency,notation:"compact",maximumFractionDigits:1});return <section className="rounded-3xl border border-white/[0.075] bg-white/[0.025] p-5 sm:p-6" aria-labelledby="pipeline-heading"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-medium uppercase tracking-[.18em] text-cyan-300">Sales velocity</p><h2 id="pipeline-heading" className="mt-2 text-xl font-semibold">Sales Pipeline</h2></div><Link href="/leadestate/deals" className="flex items-center gap-1 text-xs text-slate-500 hover:text-cyan-200">View all <ArrowUpRight className="size-3.5"/></Link></div><div className="mt-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-6">{items.map((item,index)=><Link href={item.href} key={item.id} className="group focus-ring relative min-h-40 overflow-hidden rounded-2xl border border-white/[0.07] bg-black/15 p-4 hover:-translate-y-0.5 hover:border-cyan-300/25 hover:bg-cyan-300/[0.03]"><span className="absolute inset-x-0 top-0 h-0.5 bg-gradient-to-r from-cyan-300/30 to-violet-400/30" style={{opacity:.35+index*.1}}/><div className="flex items-start justify-between gap-2"><p className="text-sm font-medium text-slate-300">{item.label}</p><span className="rounded-full bg-white/[0.06] px-2 py-0.5 text-xs text-slate-400">{item.count}</span></div><p className="mt-7 text-xl font-semibold">{format.format(item.value)}</p><p className={`mt-2 flex items-center gap-1 text-xs ${item.trend>=0?"text-emerald-300":"text-rose-300"}`}>{item.trend>=0?<TrendingUp className="size-3"/>:<TrendingDown className="size-3"/>}{Math.abs(item.trend)}% this week</p><span className="mt-5 block text-[11px] text-slate-600 group-hover:text-cyan-300">Filter deals →</span></Link>)}</div></section>}
