import"server-only";import{TaskService}from"./task.service";import{MeetingService}from"./meeting.service";import{SiteVisitService}from"./site-visit.service";import type{CalendarEntry}from"../types";export class CalendarService{async entries():Promise<CalendarEntry[]>{const[tasks,meetings,visits]=await Promise.all([new TaskService().list(),new MeetingService().list(),new SiteVisitService().list()]);return[...tasks.map(t=>({id:t.id,entryType:"task"as const,title:t.title,startsAt:t.dueAt,status:t.status})),...meetings.map(m=>({id:m.id,entryType:"meeting"as const,title:m.title,startsAt:m.startsAt,endsAt:m.endsAt,status:m.status})),...visits.map(v=>({id:v.id,entryType:"site_visit"as const,title:"Property site visit",startsAt:v.startsAt,status:v.status}))].sort((a,b)=>a.startsAt.localeCompare(b.startsAt))}}
