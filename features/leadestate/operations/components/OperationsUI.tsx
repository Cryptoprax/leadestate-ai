import Link from"next/link";import type{ActivityRecord,CalendarEntry,MeetingRecord,OperationsSnapshot,SiteVisitRecord,TaskRecord}from"../types";const card="rounded-3xl border border-white/[0.08] bg-white/[0.03] p-5";export function OperationsHeader({eyebrow,title,description,action}:{eyebrow:string;title:string;description:string;action?:{href:string;label:string}}){return <header className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-medium uppercase tracking-[.22em] text-cyan-300">{eyebrow}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">{title}</h1><p className="mt-2 max-w-2xl text-sm text-slate-400">{description}</p></div>{action&&<Link className="rounded-full bg-cyan-300 px-5 py-3 text-sm font-semibold text-slate-950" href={action.href}>{action.label}</Link>}</header>}export function OperationsDashboard({data}:{data:OperationsSnapshot}){const today=new Date().toISOString().slice(0,10);const stats=[["Today's Tasks",data.tasks.filter(x=>x.dueAt.startsWith(today)).length],["Today's Meetings",data.meetings.filter(x=>x.startsAt.startsWith(today)).length],["Today's Site Visits",data.visits.filter(x=>x.startsAt.startsWith(today)).length],["Pending Follow-ups",data.tasks.filter(x=>x.status==="pending").length],["Hot Leads",data.hotLeads],["Deals Closing Soon",data.closingDeals],["Properties Added Today",data.propertiesToday],["Upcoming Payments","—"]];return <><section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{stats.map(([label,value])=><article className={card} key={label}><p className="text-xs uppercase tracking-wider text-slate-500">{label}</p><p className="mt-4 text-3xl font-semibold">{value}</p></article>)}</section><section className="mt-5 grid gap-5 lg:grid-cols-[1.4fr_1fr]"><ActivityFeed items={data.activity}/><article className={card}><h2 className="font-semibold">Pipeline snapshot</h2><div className="mt-4 space-y-3">{Object.entries(data.pipeline).map(([stage,count])=><div className="flex items-center justify-between text-sm" key={stage}><span className="capitalize text-slate-400">{stage.replaceAll("_"," ")}</span><span>{count}</span></div>)}{!Object.keys(data.pipeline).length&&<Empty label="No active deals"/>}</div></article></section></>}export function TaskList({items}:{items:TaskRecord[]}){return <div className="mt-6 space-y-3">{items.map(t=><article className={`${card} flex items-center justify-between gap-4`} key={t.id}><div><p className="font-medium">{t.title}</p><p className="mt-1 text-xs text-slate-500">{t.priority} · {new Date(t.dueAt).toLocaleString()}</p></div><span className="rounded-full border border-white/10 px-3 py-1 text-xs capitalize">{t.status.replace("_"," ")}</span></article>)}{!items.length&&<Empty label="Your work queue is clear."/>}</div>}export function CalendarBoard({items,view}:{items:CalendarEntry[];view:string}){return <section className={`${card} mt-6`}><div className="flex gap-2">{["day","week","month","agenda"].map(v=><Link className={`rounded-full px-4 py-2 text-xs ${view===v?"bg-cyan-300 text-slate-950":"bg-white/5 text-slate-400"}`} href={`?view=${v}`} key={v}>{v}</Link>)}</div><div className="mt-6 divide-y divide-white/[0.06]">{items.map(i=><div className="grid gap-2 py-4 sm:grid-cols-[10rem_1fr_auto]" key={`${i.entryType}-${i.id}`}><time className="text-sm text-cyan-300">{new Date(i.startsAt).toLocaleString()}</time><span>{i.title}</span><span className="text-xs uppercase text-slate-500">{i.entryType.replace("_"," ")}</span></div>)}{!items.length&&<Empty label="Nothing scheduled yet."/>}</div></section>}export function MeetingList({items}:{items:MeetingRecord[]}){return <Simple items={items.map(x=>({id:x.id,title:x.title,meta:`${x.meetingType.replace("_"," ")} · ${new Date(x.startsAt).toLocaleString()}`}))}/>}export function VisitList({items}:{items:SiteVisitRecord[]}){return <Simple items={items.map(x=>({id:x.id,title:`Property ${x.propertyId.slice(0,8)}`,meta:`${x.status} · ${new Date(x.startsAt).toLocaleString()}`}))}/>}export function ActivityFeed({items}:{items:ActivityRecord[]}){return <article className={card}><h2 className="font-semibold">Recent activity</h2><div className="mt-4 space-y-4">{items.map(i=><div className="border-l border-cyan-300/30 pl-4" key={i.id}><p className="text-sm">{i.title}</p><p className="mt-1 text-xs text-slate-500">{new Date(i.occurredAt).toLocaleString()}</p></div>)}{!items.length&&<Empty label="Activity will appear as your team works."/>}</div></article>}function Simple({items}:{items:{id:string;title:string;meta:string}[]}){return <div className="mt-6 grid gap-3 md:grid-cols-2">{items.map(i=><article className={card} key={i.id}><p className="font-medium">{i.title}</p><p className="mt-2 text-xs capitalize text-slate-500">{i.meta}</p></article>)}{!items.length&&<Empty label="No records scheduled."/>}</div>}function Empty({label}:{label:string}){return <p className="py-8 text-center text-sm text-slate-500">{label}</p>}
