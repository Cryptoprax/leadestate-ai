import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";import type{ActivityRecord}from"../types";export class ActivityRepository{constructor(private c:SupabaseClient,private o:string,private w:string){}async list(limit=40){const{data,error}=await this.c.from("activity_events").select("id,event_type,title,description,occurred_at,actor_id,related_type,related_id").eq("organization_id",this.o).eq("workspace_id",this.w).order("occurred_at",{ascending:false}).limit(limit);if(error)throw error;return(data??[]).map(r=>({id:r.id,eventType:r.event_type,title:r.title,description:r.description??undefined,occurredAt:r.occurred_at,actorId:r.actor_id??undefined,relatedType:r.related_type??undefined,relatedId:r.related_id??undefined}))as ActivityRecord[]}}
