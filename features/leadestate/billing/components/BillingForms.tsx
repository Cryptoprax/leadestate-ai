import{cancelSubscriptionAction,generateInvoicePlaceholderAction,updateBillingContactAction}from"../actions/billing.actions";import type{BillingContactRecord,SubscriptionRecord}from"../types";const field="rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-sm";export function BillingContactForm({contact}:{contact:BillingContactRecord|null}){return <form action={updateBillingContactAction} className="grid gap-3 rounded-3xl border border-white/[.08] bg-white/[.03] p-5"><input className={field} name="companyName" defaultValue={contact?.companyName} placeholder="Legal company name" required/><input className={field} name="billingEmail" type="email" defaultValue={contact?.billingEmail} placeholder="Billing email" required/><input className={field} name="taxId" defaultValue={contact?.taxId} placeholder="GST / Tax ID (placeholder)"/><div className="rounded-xl border border-dashed border-white/10 p-4 text-sm text-slate-500">Payment method placeholder — available when a provider is connected.</div><button className="rounded-xl bg-cyan-300 px-5 py-3 font-semibold text-slate-950">Save billing details</button></form>}export function CancelSubscription({subscription}:{subscription:SubscriptionRecord|null}){if(!subscription)return null;return <form action={cancelSubscriptionAction}><input type="hidden" name="expectedVersion" value={subscription.version}/><button className="rounded-xl border border-rose-300/20 px-4 py-3 text-sm text-rose-200">Cancel at period end</button></form>}export function GenerateDraftInvoice(){return <form action={generateInvoicePlaceholderAction}><button className="rounded-xl border border-white/10 px-4 py-3 text-sm text-slate-300">Generate draft invoice</button></form>}
