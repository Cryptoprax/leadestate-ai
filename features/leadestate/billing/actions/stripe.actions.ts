"use server";import{headers}from"next/headers";import{redirect}from"next/navigation";import{StripeService}from"../services/stripe.service";async function origin(){const h=await headers();return h.get("origin")??process.env.NEXT_PUBLIC_APP_URL??"http://localhost:3000"}export async function createCheckoutSessionAction(form:FormData){const plan=String(form.get("planCode")??""),seats=Math.max(1,Number(form.get("seatQuantity")??1)),{url}=await new StripeService().checkout(plan,seats,await origin());redirect(url)}export async function openBillingPortalAction(){const{url}=await new StripeService().portal(await origin());redirect(url)}
