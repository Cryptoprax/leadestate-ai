# Property Engine

The Property Engine consumes LeadEstate domain contracts and AtlasOS catalogs. Pages orchestrate, services authorize, repositories scope every query to organization and workspace, and mutations use atomic Supabase RPCs. Property deletion is always a soft archive. Media, documents, offers, marketing, leads, activity, analytics, and AI tabs are extension-ready presentation boundaries.

