export interface ExtractionPipeline<TInput,TOutput>{readonly id:string;readonly available:boolean;extract(input:TInput):Promise<TOutput>}export class OcrExtractionPipeline implements ExtractionPipeline<File,never>{readonly id="ocr";readonly available=false;async extract():Promise<never>{throw new Error("OCR provider is not configured.")}}export class VisionExtractionPipeline implements ExtractionPipeline<File[],never>{readonly id="vision";readonly available=false;async extract():Promise<never>{throw new Error("Vision provider is not configured.")}}
