import type{ReactNode}from"react";import{Suspense}from"react";import{ProductExperience}from"./ProductExperience";import{leadEstateNavigation}from"@/features/platform/builder/config/leadestate-navigation";import{logoutAction}from"@/features/authentication/actions/auth.actions";export function LeadEstateShell({children}:{children:ReactNode}){return <div className="min-h-dvh bg-[#060a10] text-white" data-navigation-items={leadEstateNavigation.filter(item=>item.visible).length}><Suspense><ProductExperience>{children}</ProductExperience></Suspense><footer className="mx-auto flex max-w-[100rem] items-center justify-between border-t border-white/[.06] px-5 py-8 text-xs text-slate-600"><span>LeadEstate AI</span><form action={logoutAction}><button className="focus-ring rounded-lg px-3 py-2 hover:bg-white/5 hover:text-white">Sign out</button></form></footer></div>}
