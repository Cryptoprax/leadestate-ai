# Authentication

Authentication uses Supabase SSR clients, PKCE callbacks, server actions, secure framework-managed cookies, server-side getUser verification, and proxy session refresh. Error responses avoid account enumeration. Application authorization remains separate from authentication.

