import{dismissNotificationAction}from"../actions";import type{NotificationRecord}from"../types";export function NotificationCenter({items}:{items:NotificationRecord[]}){return <div className="mt-6 grid gap-3">{items.map(x=><article key={x.id} className="rounded-2xl border border-white/10 bg-white/[.035] p-4"><div className="flex items-start justify-between gap-4"><div><p className="text-xs uppercase tracking-wider text-cyan-300">{x.category} · {x.priority}</p><h2 className="mt-1 font-semibold">{x.title}</h2><p className="mt-1 text-sm text-slate-400">{x.body}</p></div><form action={dismissNotificationAction}><input type="hidden" name="notificationId" value={x.id}/><button className="text-sm text-slate-500 hover:text-white">Dismiss</button></form></div></article>)}{!items.length&&<p className="rounded-2xl border border-dashed border-white/10 p-10 text-center text-slate-500">You are all caught up.</p>}</div>}
