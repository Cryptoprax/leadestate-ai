import { Button } from "@/features/platform/design-system";
import{dismissNotificationAction}from"../actions";import type{NotificationRecord}from"../types";export function NotificationCenter({items}:{items:NotificationRecord[]}){return <div className="mt-6 grid gap-3">{items.map(x=><article key={x.id} className="rounded-2xl border border-vds-border bg-vds-surface/[.035] p-4"><div className="flex items-start justify-between gap-4"><div><p className="text-xs uppercase tracking-wider text-vds-primary">{x.category} · {x.priority}</p><h2 className="mt-1 font-semibold">{x.title}</h2><p className="mt-1 text-sm text-vds-muted">{x.body}</p></div><form action={dismissNotificationAction}><input type="hidden" name="notificationId" value={x.id}/><Button variant="control" className="text-sm text-vds-muted hover:text-vds-foreground">Dismiss</Button></form></div></article>)}{!items.length&&<p className="rounded-2xl border border-dashed border-vds-border p-10 text-center text-vds-muted">You are all caught up.</p>}</div>}
