import{z}from"zod";import{notificationCategories,notificationChannels,notificationPriorities,reminderKinds}from"../types";
export const notificationEnvelopeSchema=z.object({organizationId:z.string().uuid(),workspaceId:z.string().uuid(),userId:z.string().uuid().optional(),category:z.enum(notificationCategories),title:z.string().trim().min(1).max(180),body:z.string().trim().min(1).max(4000),priority:z.enum(notificationPriorities).default("normal"),channels:z.array(z.enum(notificationChannels)).min(1).max(5).default(["in_app"]),dedupeKey:z.string().trim().min(1).max(220),sourceType:z.string().trim().max(80).optional(),sourceId:z.string().uuid().optional(),mentioned:z.boolean().optional()});
export const preferenceSchema=z.object({email:z.boolean(),inApp:z.boolean(),browserPush:z.boolean(),whatsapp:z.boolean(),webhook:z.boolean(),muted:z.boolean(),quietHoursStart:z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/).optional(),quietHoursEnd:z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/).optional(),digestFrequency:z.enum(["instant","daily","weekly","off"]),categories:z.partialRecord(z.enum(notificationCategories),z.boolean())});
export const reminderSchema=z.object({kind:z.enum(reminderKinds),title:z.string().trim().min(1).max(180),remindAt:z.string().datetime(),sourceType:z.string().trim().max(80).optional(),sourceId:z.string().uuid().optional()});
