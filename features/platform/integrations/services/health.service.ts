import"server-only";import{integrationContext}from"./integration-context";import{HealthRepository}from"../repositories/health.repository";export class HealthService{private async repo(){const c=await integrationContext();return new HealthRepository(c.client,c.organizationId,c.workspaceId)}async health(){return(await this.repo()).health()}async logs(){return(await this.repo()).logs()}async retries(){return(await this.repo()).retries()}async archiveLog(id:string,version:number){return(await this.repo()).archiveLog(id,version)}}
