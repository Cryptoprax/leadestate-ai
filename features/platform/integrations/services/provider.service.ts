import"server-only";import{integrationContext}from"./integration-context";import{ProviderRepository}from"../repositories/provider.repository";export class ProviderService{private async repo(){const c=await integrationContext();return new ProviderRepository(c.client,c.organizationId,c.workspaceId)}async providers(){return(await this.repo()).providers()}async connections(){return(await this.repo()).connections()}async enable(id:string,version:number){return(await this.repo()).setEnabled(id,version,true)}async disable(id:string,version:number){return(await this.repo()).setEnabled(id,version,false)}}
