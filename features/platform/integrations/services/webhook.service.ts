import"server-only";import{integrationContext}from"./integration-context";import{WebhookRepository}from"../repositories/webhook.repository";export class WebhookService{private async repo(){const c=await integrationContext();return new WebhookRepository(c.client,c.organizationId,c.workspaceId)}async list(){return(await this.repo()).list()}async replay(id:string,version:number){return(await this.repo()).replay(id,version)}}
