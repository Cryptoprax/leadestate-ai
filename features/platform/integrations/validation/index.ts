import{z}from"zod";export const connectionActionSchema=z.object({connectionId:z.string().uuid(),expectedVersion:z.coerce.number().int().positive()});export const rotateMetadataSchema=z.object({metadataId:z.string().uuid(),expectedVersion:z.coerce.number().int().positive()});export const webhookReplaySchema=z.object({webhookId:z.string().uuid(),expectedVersion:z.coerce.number().int().positive()});export const retrySchema=z.object({retryId:z.string().uuid(),expectedVersion:z.coerce.number().int().positive()});export const archiveLogSchema=z.object({logId:z.string().uuid(),expectedVersion:z.coerce.number().int().positive()});
