import"server-only";import{GoogleOAuthService}from"./google-oauth.service";export class GoogleApiService{constructor(private oauth=new GoogleOAuthService()){}async request<T>(url:string,init:RequestInit={}){const token=await this.oauth.accessToken(),response=await fetch(url,{...init,headers:{authorization:`Bearer ${token}`,"content-type":"application/json",...init.headers},cache:"no-store"});if(!response.ok)throw new Error(`Google API request failed (${response.status}).`);return response.status===204?undefined as T:await response.json()as T}}
