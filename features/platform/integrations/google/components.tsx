"use client";
import { Button } from "@/features/platform/design-system";import{TimezoneSelect}from"@/features/location/components";import{connectGoogleAction,createDraftAction,sendEmailAction,createGoogleMeetingAction}from"./actions";import type{CalendarEvent,GmailMessage}from"./types";const card="rounded-2xl border border-vds-border bg-vds-surface/[.035] p-5";export function GoogleConnectCard({connected,email}:{connected:boolean;email?:string}){return <section className={card}><p className="text-xs font-semibold uppercase tracking-[.2em] text-vds-primary">Google Workspace</p><h2 className="mt-2 text-xl font-semibold">{connected?"Connected":"Connect your workspace"}</h2><p className="mt-2 text-sm text-vds-muted">{connected?email:"Authorize identity access with Google OAuth. Vayon never stores your Google password."}</p>{!connected&&<form action={connectGoogleAction} className="mt-5"><Button variant="control" className="rounded-xl bg-vds-surface px-4 py-2 text-sm font-semibold text-vds-foreground">Connect Google</Button></form>}</section>}export function Inbox({messages}:{messages:GmailMessage[]}){return <section className={card}><h2 className="font-semibold">Inbox</h2><div className="mt-4 divide-y divide-vds-divider">{messages.length?messages.map(m=><article key={m.id} className="py-3"><div className="flex justify-between gap-3"><p className="truncate font-medium">{m.subject||"(no subject)"}</p><time className="text-xs text-vds-muted">{m.date}</time></div><p className="mt-1 text-sm text-vds-muted">{m.from}</p><p className="mt-1 line-clamp-2 text-sm text-vds-muted">{m.snippet}</p></article>):<p className="py-8 text-sm text-vds-muted">No messages match this view.</p>}</div></section>}export function EmailComposer(){return <section className={card}><h2 className="font-semibold">Compose email</h2><form className="mt-4 space-y-3"><input name="to" type="email" required placeholder="Recipient" aria-label="Recipient" className="w-full rounded-xl border border-vds-border bg-vds-input px-3 py-2"/><input name="subject" required placeholder="Subject" aria-label="Subject" className="w-full rounded-xl border border-vds-border bg-vds-input px-3 py-2"/><textarea name="body" required rows={8} placeholder="Write a message" aria-label="Message" className="w-full rounded-xl border border-vds-border bg-vds-input px-3 py-2"/><div className="flex gap-2"><Button variant="control" formAction={createDraftAction} className="rounded-xl border border-vds-border-strong px-4 py-2 text-sm">Save draft</Button><Button variant="primary" formAction={sendEmailAction} className="rounded-xl px-4 py-2 text-sm font-semibold">Send</Button></div></form></section>}export function CalendarView({events}:{events:CalendarEvent[]}){return <section className={card}><h2 className="font-semibold">Google Calendar</h2><div className="mt-4 grid gap-3">{events.length?events.map(e=><article key={e.id} className="rounded-xl border border-vds-border p-3"><p className="font-medium">{e.summary}</p><p className="mt-1 text-sm text-vds-muted">{new Date(e.start).toLocaleString()} — {new Date(e.end).toLocaleTimeString()}</p>{e.location&&<p className="mt-1 text-xs text-vds-muted">{e.location}</p>}</article>):<p className="py-8 text-sm text-vds-muted">No upcoming Google events.</p>}</div></section>}export function CalendarCard(){return <section className={card}><h2 className="font-semibold">Schedule a Google meeting</h2><form action={createGoogleMeetingAction} className="mt-4 grid gap-3 sm:grid-cols-2"><input name="summary" required placeholder="Meeting title" aria-label="Meeting title" className="rounded-xl border border-vds-border bg-vds-input px-3 py-2 sm:col-span-2"/><input name="start" type="datetime-local" required aria-label="Start" className="rounded-xl border border-vds-border bg-vds-input px-3 py-2"/><input name="end" type="datetime-local" required aria-label="End" className="rounded-xl border border-vds-border bg-vds-input px-3 py-2"/><TimezoneSelect name="timeZone" label="Timezone" required/><input name="attendees" placeholder="Emails, comma separated" aria-label="Attendees" className="rounded-xl border border-vds-border bg-vds-input px-3 py-2"/><Button variant="primary" className="rounded-xl px-4 py-2 text-sm font-semibold sm:col-span-2">Create and invite</Button></form></section>}
