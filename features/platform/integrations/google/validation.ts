import{z}from"zod";export const emailDraftSchema=z.object({to:z.string().email(),subject:z.string().min(1).max(200),body:z.string().min(1).max(50_000),threadId:z.string().optional(),inReplyTo:z.string().optional()});export const calendarEventSchema=z.object({summary:z.string().min(1).max(200),description:z.string().max(10_000).optional(),start:z.string().datetime(),end:z.string().datetime(),timeZone:z.string().min(1),location:z.string().max(500).optional(),attendees:z.array(z.string().email()).max(100).default([])}).refine(v=>new Date(v.end)>new Date(v.start),"End must be after start.");
