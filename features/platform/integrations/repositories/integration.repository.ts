import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";export class IntegrationRepository{constructor(private c:SupabaseClient,private o:string,private w:string){}async syncHistory(){const{data,error}=await this.c.from("integration_sync_history").select("id,provider_id,entity_type,direction,started_at,completed_at,duration_ms,status").eq("organization_id",this.o).eq("workspace_id",this.w).order("started_at",{ascending:false}).limit(100);if(error)throw error;return data??[]}}
