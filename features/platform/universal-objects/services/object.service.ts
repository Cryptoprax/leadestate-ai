import type{UniversalActivity,UniversalContact,UniversalObject,UniversalRelationship,UniversalTag}from"../domain/models";import type{RelationshipStore,UniversalObjectStore}from"../storage/contracts";
export class UniversalObjectService{constructor(private objects:UniversalObjectStore,private relationships:RelationshipStore){}list(){return this.objects.list()}profile(id:string){return{object:this.objects.get(id),relationships:this.relationships.list(id)}}save(object:UniversalObject){this.objects.save(object)}relate(source:UniversalObject["type"] extends never?never:UniversalRelationship){this.relationships.save(source)}duplicateContacts(candidate:UniversalContact){return this.objects.list(["contact"]).filter(item=>item.type==="contact"&&item.id!==candidate.id&&(Boolean(candidate.email&&item.email.toLowerCase()===candidate.email.toLowerCase())||Boolean(candidate.phone&&item.phone===candidate.phone))) as UniversalContact[]}mergeContacts(primary:UniversalContact,duplicate:UniversalContact):UniversalContact{return{...primary,alternatePhone:primary.alternatePhone||duplicate.phone,whatsapp:primary.whatsapp||duplicate.whatsapp,tags:[...new Set([...primary.tags,...duplicate.tags])],notes:[primary.notes,duplicate.notes].filter(Boolean).join("\n\n"),relationships:[...primary.relationships,...duplicate.relationships],relationshipCount:primary.relationshipCount+duplicate.relationshipCount,metadata:{...duplicate.metadata,...primary.metadata},updatedAt:new Date().toISOString()}}}
export function groupActivities(items:readonly UniversalActivity[]){return Object.entries(Object.groupBy([...items].sort((a,b)=>b.occurredAt.localeCompare(a.occurredAt)),item=>new Date(item.occurredAt).toDateString()))}
export function mergeTags(tags:readonly UniversalTag[],sourceId:string,targetId:string){return tags.map(tag=>tag.id===sourceId?{...tag,mergedInto:targetId,status:"merged",updatedAt:new Date().toISOString()}:tag)}
