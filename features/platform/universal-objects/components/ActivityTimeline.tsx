import type{UniversalActivity}from"../domain/models";import{groupActivities}from"../services/object.service";import{TimelineCard}from"./TimelineCard";
export function ActivityTimeline({items}:{items:readonly UniversalActivity[]}){return <section><h2 className="text-xl font-semibold">Universal Activity</h2><p className="mt-1 text-sm text-slate-500">Lead, deal, calendar, communication, document, note, task, and AI events in one chronology.</p><div className="mt-5 space-y-6">{groupActivities(items).map(([date,activities])=><div key={date}><p className="mb-4 text-xs font-semibold uppercase tracking-wider text-slate-500">{date}</p>{activities?.map(item=><TimelineCard key={item.id} activity={item}/>)}</div>)}{!items.length&&<p className="rounded-2xl border border-dashed border-white/10 p-8 text-center text-sm text-slate-600">Timeline events will appear here when objects are linked.</p>}</div></section>}
