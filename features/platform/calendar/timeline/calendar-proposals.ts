export const calendarTimelineMappings=Object.freeze({created:"meeting.created",updated:"meeting.updated",cancelled:"meeting.cancelled",accepted:"meeting.invite.accepted",declined:"meeting.invite.declined",reminder:"meeting.reminder.triggered"}as const);export interface CalendarTimelineProposal{readonly eventType:(typeof calendarTimelineMappings)[keyof typeof calendarTimelineMappings];readonly workspaceId:string;readonly eventId:string;readonly calendarId:string;readonly occurredAt:string;readonly objectReferences:readonly{type:string;id:string}[]}export function proposeCalendarEvent(input:CalendarTimelineProposal){return Object.freeze({...input,objectReferences:Object.freeze([...input.objectReferences])})}
