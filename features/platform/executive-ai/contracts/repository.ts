import type{ExecutiveObservability,ExecutiveRevenue}from"../types";export interface ExecutiveEvidence{readonly revenue:ExecutiveRevenue;readonly newLeads:number;readonly meetingsScheduled:number;readonly dealsWon:number;readonly outstandingApprovals:number;readonly overdueTasks:number;readonly timeline:readonly{id:string;summary:string;occurredAt:string}[];readonly observability:ExecutiveObservability}export type ExecutiveAIEvidence=ExecutiveEvidence;export interface ExecutiveAIRepositoryContract{evidence():Promise<ExecutiveEvidence>}
