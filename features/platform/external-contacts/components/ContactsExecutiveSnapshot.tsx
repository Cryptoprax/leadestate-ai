import{GoogleContactsService}from"@/features/platform/integrations/google/services/contacts.service";import{ContactMatcher}from"../matching/contact-matcher";type Snapshot={status:"connected"|"not_connected"|"unavailable";recent?:number;review?:number};async function load():Promise<Snapshot>{const service=new GoogleContactsService();try{if(await service.status()!=="connected")return{status:"not_connected"};const page=await service.list({view:"recent",sort:"updated"}),duplicates=new ContactMatcher().duplicates(page.contacts);return{status:"connected",recent:page.contacts.length,review:duplicates.length}}catch{return{status:"unavailable"}}}export async function ContactsExecutiveSnapshot(){const data=await load();if(data.status!=="connected")return <section className="rounded-2xl border border-vds-border p-5"><h2 className="font-semibold">Contacts snapshot</h2><p className="mt-2 text-sm text-vds-muted">{data.status==="not_connected"?"Google Contacts is not connected.":"Contact data is temporarily unavailable."}</p></section>;return <section className="rounded-2xl border border-vds-border p-5"><h2 className="font-semibold">Google Contacts snapshot</h2><dl className="mt-4 grid grid-cols-2 gap-4 text-sm"><div><dt className="text-vds-subtle">Recently connected contacts</dt><dd className="mt-1 text-xl font-semibold">{data.recent}</dd></div><div><dt className="text-vds-subtle">Contacts needing review</dt><dd className="mt-1 text-xl font-semibold">{data.review}</dd></div></dl><p className="mt-3 text-xs text-vds-subtle">Live provider page and deterministic duplicate suggestions only.</p></section>}
