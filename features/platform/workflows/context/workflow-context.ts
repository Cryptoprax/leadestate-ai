export interface WorkflowContextSlice {readonly id:"workflows";readonly workflowId:string;readonly state:"available"|"awaiting-data";readonly references:readonly {readonly objectType:string;readonly objectId:string}[];readonly message:string}
export function createWorkflowContextSlice(workflowId:string,references:WorkflowContextSlice["references"]=[]):WorkflowContextSlice{return Object.freeze({id:"workflows",workflowId,state:references.length?"available":"awaiting-data",references:Object.freeze(references),message:references.length?"Workflow references are available.":"Awaiting connected workflow context."})}
