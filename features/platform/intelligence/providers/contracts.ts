export type AIProviderId="openai"|"anthropic"|"google-gemini"|"azure-openai"|"ollama"|"local-llm";
export interface ProviderRequest{model?:string;system?:string;input:string;metadata?:Readonly<Record<string,unknown>>;signal?:AbortSignal}
export interface ProviderResponse<T=string>{output:T;provider:AIProviderId;model:string;usage?:{inputTokens:number;outputTokens:number};latencyMs?:number}
export interface AIProviderCapabilities{chat:boolean;structuredOutput:boolean;embeddings:boolean;vision:boolean;speechToText:boolean;textToSpeech:boolean;ocr:boolean;toolCalling:boolean;streaming:boolean;batch:boolean;moderation:boolean}
export interface AIProvider{readonly id:AIProviderId;readonly capabilities:AIProviderCapabilities;chat(request:ProviderRequest):Promise<ProviderResponse>;structured<T>(request:ProviderRequest,schema:unknown):Promise<ProviderResponse<T>>;embed(input:readonly string[]):Promise<readonly number[][]>;vision(request:ProviderRequest&{images:readonly string[]}):Promise<ProviderResponse>;speechToText(audio:Blob):Promise<ProviderResponse>;textToSpeech(input:string):Promise<Blob>;ocr(input:Blob):Promise<ProviderResponse>;tools(request:ProviderRequest,tools:readonly unknown[]):Promise<ProviderResponse>;stream(request:ProviderRequest):AsyncIterable<ProviderResponse>;batch(requests:readonly ProviderRequest[]):Promise<readonly ProviderResponse[]>;moderate(input:string):Promise<{flagged:boolean;categories:readonly string[]}>}
export interface ProviderPolicy{retry:{enabled:boolean;maxAttempts:number};rateLimit:{requests:number;windowMs:number};timeoutMs:number}
export interface AIProviderRegistry{register(provider:AIProvider):void;resolve(id:AIProviderId):AIProvider;available():readonly AIProviderId[]}
