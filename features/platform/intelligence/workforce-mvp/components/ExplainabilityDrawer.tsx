import { Button } from "@/features/platform/design-system";
import { X } from "lucide-react";
import type { AdvisorViewModel } from "../view-models/advisor";
export function ExplainabilityDrawer({ open, advisor, onClose }: { open: boolean; advisor: AdvisorViewModel; onClose: () => void }) { if (!open) return null; return <div className="fixed inset-0 z-50 flex justify-end bg-vds-overlay/60 backdrop-blur-sm" role="presentation" onMouseDown={event => { if (event.target === event.currentTarget) onClose() }}><aside role="dialog" aria-modal="true" aria-labelledby="explanation-title" className="h-full w-full max-w-lg overflow-y-auto border-l border-vds-border bg-[var(--vds-color-background)] p-6 shadow-2xl"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[.18em] text-vds-primary">Explainability</p><h2 id="explanation-title" className="mt-1 text-xl font-semibold">Why this assessment</h2></div><Button variant="control" type="button" onClick={onClose} className="focus-ring grid size-10 place-items-center rounded-xl text-vds-muted hover:bg-vds-elevated hover:text-vds-foreground" aria-label="Close explainability"><X className="size-5"/></Button></div><div className="mt-6 space-y-5"><section><h3 className="text-sm font-medium">Reasoning</h3><p className="mt-2 text-sm leading-6 text-vds-muted">{advisor.explanation.why}</p></section><section><h3 className="text-sm font-medium">Data sources</h3>{advisor.explanation.dataSources.length ? <ul className="mt-2 space-y-2">{advisor.explanation.dataSources.map(source => <li key={source} className="rounded-xl border border-vds-border/[.07] bg-vds-surface/[.025] px-3 py-2 text-xs text-vds-muted">{source}</li>)}</ul> : <p className="mt-2 text-sm text-vds-subtle">No governed data sources were used.</p>}</section><section><h3 className="text-sm font-medium">Citations</h3>{advisor.citations.length ? <ul className="mt-2 space-y-2">{advisor.citations.map(citation => <li key={citation.id} className="rounded-xl border border-vds-border/[.07] p-3"><p className="text-sm text-vds-secondary">{citation.label}</p><p className="mt-1 text-xs text-vds-subtle">{citation.source}</p></li>)}</ul> : <p className="mt-2 text-sm text-vds-subtle">No citations are available until context is assembled.</p>}</section><section><h3 className="text-sm font-medium">Limitations</h3><ul className="mt-2 space-y-2 text-xs leading-5 text-vds-warning/70">{advisor.explanation.limitations.map(item => <li key={item}>• {item}</li>)}</ul></section></div></aside></div> }

