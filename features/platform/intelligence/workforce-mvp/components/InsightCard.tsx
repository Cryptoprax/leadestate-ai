import { Button } from "@/features/platform/design-system";
import { CircleAlert, FileSearch, Sparkles } from "lucide-react";
import type { AdvisorInsightViewModel } from "../view-models/advisor";
export function InsightCard({ insight, onExplain }: { insight: AdvisorInsightViewModel; onExplain: () => void }) { return <article className="rounded-2xl border border-vds-border/[.08] bg-vds-surface/[.025] p-5 transition hover:-translate-y-0.5 hover:border-vds-accent-border"><div className="flex items-start justify-between gap-3"><span className="grid size-9 place-items-center rounded-xl bg-vds-primary/[.07] text-vds-primary">{insight.placeholder ? <FileSearch className="size-4" aria-hidden="true"/> : insight.tone === "critical" ? <CircleAlert className="size-4" aria-hidden="true"/> : <Sparkles className="size-4" aria-hidden="true"/>}</span><span className="rounded-full border border-vds-border/[.07] px-2 py-1 text-[9px] uppercase tracking-wide text-vds-subtle">{insight.placeholder ? "Awaiting context" : "Advisory"}</span></div><h3 className="mt-4 font-medium capitalize">{insight.title}</h3><p className="mt-2 min-h-10 text-xs leading-5 text-vds-muted">{insight.summary}</p><div className="mt-4"><div className="flex justify-between text-[10px] text-vds-subtle"><span>Confidence</span><span>{Math.round(insight.confidence.score * 100)}%</span></div><div className="mt-1 h-1 overflow-hidden rounded-full bg-vds-surface/[.06]"><div className="h-full rounded-full bg-vds-primary" style={{ width: `${insight.confidence.score * 100}%` }}/></div></div><Button variant="control" type="button" onClick={onExplain} className="focus-ring mt-4 rounded-lg text-xs text-vds-primary hover:text-vds-primary">Why this assessment · {insight.citationCount} sources</Button></article> }

