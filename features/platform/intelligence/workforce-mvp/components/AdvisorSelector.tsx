import { Button } from "@/features/platform/design-system";
import { BriefcaseBusiness, Building2, ChartNoAxesCombined } from "lucide-react";
import type { AdvisorDefinition, AdvisorType } from "../domain/contracts";
const icons = { executive: ChartNoAxesCombined, sales: BriefcaseBusiness, operations: Building2 } as const;
export function AdvisorSelector({ advisors, active, onSelect }: { advisors: readonly AdvisorDefinition[]; active: AdvisorType; onSelect: (advisor: AdvisorType) => void }) { return <div className="grid gap-2 sm:grid-cols-3" role="tablist" aria-label="Select an advisor">{advisors.map(advisor => { const Icon = icons[advisor.id], selected = advisor.id === active; return <Button variant="control" key={advisor.id} type="button" role="tab" aria-selected={selected} onClick={() => onSelect(advisor.id)} className={`focus-ring rounded-2xl border p-4 text-left transition ${selected ? "border-vds-accent-border bg-vds-primary/[.08] shadow-[0_0_30px_var(--vds-color-primary-soft)]" : "border-vds-border/[.07] bg-vds-surface/[.02] hover:border-vds-border-strong hover:bg-vds-surface/[.04]"}`}><span className={`grid size-9 place-items-center rounded-xl ${selected ? "bg-vds-primary-soft text-vds-primary" : "bg-vds-surface/[.05] text-vds-muted"}`}><Icon className="size-4" aria-hidden="true"/></span><span className="mt-3 block text-sm font-medium text-vds-foreground">{advisor.name}</span><span className="mt-1 block text-xs leading-5 text-vds-muted">{advisor.description}</span></Button> })}</div> }

