import { Clock3 } from "lucide-react";
import type { RecommendationTimelineEntry } from "../domain/contracts";
export function RecommendationTimeline({ entries }: { entries: readonly RecommendationTimelineEntry[] }) { return <section aria-labelledby="timeline-title" className="rounded-3xl border border-vds-border/[.08] bg-vds-surface/[.02] p-5"><div className="flex items-center gap-2"><Clock3 className="size-4 text-vds-accent" aria-hidden="true"/><h2 id="timeline-title" className="font-semibold">Recommendation timeline</h2></div>{entries.length ? <ol className="mt-4 space-y-3">{entries.map(entry => <li key={entry.id} className="border-l border-vds-accent pl-4"><p className="text-sm text-vds-secondary">{entry.title}</p><p className="mt-1 text-xs leading-5 text-vds-subtle">{entry.description}</p></li>)}</ol> : <div className="mt-4 rounded-2xl border border-dashed border-vds-border/[.08] p-6 text-center"><p className="text-sm text-vds-muted">No recommendations yet</p><p className="mt-1 text-xs text-vds-subtle">Approved advisory output will appear here in future releases.</p></div>}</section> }

