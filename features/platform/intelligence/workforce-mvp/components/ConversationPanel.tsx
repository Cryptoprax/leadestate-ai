import { Button } from "@/features/platform/design-system";
import { SendHorizontal } from "lucide-react";
import type { ConversationMessage } from "../domain/contracts";
export function ConversationPanel({ messages, value, onChange, onSubmit }: { messages: readonly ConversationMessage[]; value: string; onChange: (value: string) => void; onSubmit: () => void }) { return <section aria-labelledby="conversation-title" className="rounded-3xl border border-vds-border/[.08] bg-vds-surface/[.02] p-5"><div><h2 id="conversation-title" className="font-semibold">Advisor conversation</h2><p className="mt-1 text-xs text-vds-muted">Architecture preview—messages remain in this page session and trigger no analysis.</p></div><div className="mt-4 min-h-44 space-y-3 rounded-2xl border border-vds-border/[.06] bg-vds-input p-3" aria-live="polite">{messages.length === 0 ? <div className="grid min-h-36 place-items-center text-center"><div><p className="text-sm text-vds-muted">Ask for an advisory review</p><p className="mt-1 text-xs text-vds-subtle">Provider execution is not connected.</p></div></div> : messages.map(message => <div key={message.id} className={`max-w-[88%] rounded-2xl px-3 py-2 text-xs leading-5 ${message.role === "user" ? "ml-auto bg-vds-primary text-vds-on-accent" : "border border-vds-border/[.07] bg-vds-surface/[.04] text-vds-muted"}`}><p>{message.content}</p></div>)}</div><form className="mt-3 flex gap-2" onSubmit={event => { event.preventDefault(); onSubmit() }}><label htmlFor="workforce-message" className="sr-only">Message advisor</label><input id="workforce-message" value={value} onChange={event => onChange(event.target.value)} placeholder="Ask for an advisory review…" className="focus-ring min-w-0 flex-1 rounded-xl border border-vds-border bg-vds-input px-3 py-2.5 text-sm text-vds-foreground placeholder:text-vds-subtle"/><Button variant="primary" type="submit" disabled={!value.trim()} className="focus-ring grid size-10 place-items-center rounded-xl disabled:opacity-40" aria-label="Add advisory prompt"><SendHorizontal className="size-4"/></Button></form></section> }

