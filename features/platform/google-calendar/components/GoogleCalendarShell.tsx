import Link from "next/link";import type{ReactNode}from"react";
const links=[['Overview','/vayon/calendar/google'],['Calendars','/vayon/calendar/google/calendars'],['Events','/vayon/calendar/google/events'],['Free/busy','/vayon/calendar/google/free-busy'],['Settings','/vayon/calendar/google/settings']] as const;
export function GoogleCalendarShell({children}:{readonly children:ReactNode}){return <main className="mx-auto max-w-[96rem] px-4 py-6 sm:px-6"><header className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-vds-primary">Calendar · Google</p><h1 className="mt-1 text-3xl font-semibold">Google Calendar</h1><p className="mt-2 text-sm text-vds-muted">Live, tenant-scoped calendar data using your existing Google connection.</p></div></header><nav aria-label="Google Calendar" className="mt-6 flex gap-2 overflow-x-auto rounded-2xl border border-vds-border bg-vds-surface p-2">{links.map(([label,href])=><Link key={href} href={href} className="shrink-0 rounded-xl px-3 py-2 text-sm text-vds-muted hover:bg-vds-hover hover:text-vds-foreground">{label}</Link>)}</nav><div className="mt-5">{children}</div></main>}
