import type { CalendarDescriptor,CalendarWorkspaceEvent,GoogleCalendarHealth } from "../domain/models";
export type GoogleCalendarView="day"|"week"|"month"|"agenda";
export interface GoogleCalendarSnapshot { readonly health:GoogleCalendarHealth; readonly calendars:readonly CalendarDescriptor[]; readonly events:readonly CalendarWorkspaceEvent[]; readonly nextPageToken:string|null; readonly selectedCalendarIds:readonly string[]; readonly view:GoogleCalendarView; readonly query:string; readonly error:string|null }
export function meetMetadata(event:CalendarWorkspaceEvent){const url=event.conferenceUrl;return{provider:url?.includes("meet.google.com")?"Google Meet":url?"Conference provider":"Unavailable",joinLink:url,conferenceId:url?.includes("meet.google.com/")?url.split("meet.google.com/")[1]?.split(/[?#]/)[0]??null:null,recordingAvailability:"Unavailable" as const}}
export function eventColor(event:CalendarWorkspaceEvent,calendars:readonly CalendarDescriptor[]){return calendars.find(item=>item.id===event.calendarId)?.backgroundColor??"var(--vds-color-primary)"}
export function rangeForView(view:GoogleCalendarView,now=new Date()){const start=new Date(now),end=new Date(now);if(view==="day"){start.setHours(0,0,0,0);end.setHours(23,59,59,999)}else if(view==="week"){start.setDate(now.getDate()-now.getDay());start.setHours(0,0,0,0);end.setTime(start.getTime()+7*86400000)}else if(view==="month"){start.setDate(1);start.setHours(0,0,0,0);end.setMonth(start.getMonth()+1)}else{start.setHours(0,0,0,0);end.setMonth(end.getMonth()+3)}return{timeMin:start.toISOString(),timeMax:end.toISOString()}}
