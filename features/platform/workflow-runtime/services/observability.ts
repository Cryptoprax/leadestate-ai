export interface RuntimeMetric{readonly name:string;readonly sessionId:string;readonly value:number;readonly unit:"count"|"milliseconds";readonly attributes:Readonly<Record<string,string|number|boolean>>}export class WorkflowRuntimeObservability{private values:RuntimeMetric[]=[];record(metric:RuntimeMetric){this.values.push(Object.freeze({...metric,attributes:Object.freeze({...metric.attributes})}))}metrics(){return Object.freeze([...this.values])}trace(sessionId:string,stepId:string,correlationId:string){return Object.freeze({traceId:correlationId,spanId:`${sessionId}:${stepId}`,containsSecrets:false})}}
