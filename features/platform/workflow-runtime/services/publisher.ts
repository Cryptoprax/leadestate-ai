import type{RuntimeAuthorizer,WorkflowPublisher}from"../contracts/ports";import type{ExecutionContext,RuntimePlan}from"../domain/contracts";export class GovernedWorkflowPublisher implements WorkflowPublisher{constructor(private authorizer:RuntimeAuthorizer){}async publish(plan:RuntimePlan,context:ExecutionContext){if(!await this.authorizer.authorize(context,"workflow.publish"))throw new Error("Workflow publishing permission denied.");if(!plan.steps.length)throw new Error("Published workflow requires at least one step.");if(new Set(plan.steps.map(step=>step.id)).size!==plan.steps.length)throw new Error("Published workflow has duplicate step IDs.");return Object.freeze(structuredClone({...plan,published:true as const}))}}
