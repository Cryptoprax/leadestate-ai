import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";import type{HealthRecord}from"../types";export class HealthRepository{constructor(private c:SupabaseClient){}async customers(){const{data,error}=await this.c.from("customer_health").select("id,organization_id,score,churn_risk,renewal_at,open_issues,expansion_opportunity,updated_at,version").is("deleted_at",null).order("score");if(error)throw error;return(data??[]).map(r=>({id:r.id,organizationId:r.organization_id,score:r.score,churnRisk:r.churn_risk,renewalAt:r.renewal_at??undefined,openIssues:r.open_issues,expansionOpportunity:r.expansion_opportunity??undefined,updatedAt:r.updated_at,version:r.version}))as HealthRecord[]}async addNote(input:unknown){const{error}=await this.c.rpc("add_customer_health_note",{p_input:input});if(error)throw error}}
