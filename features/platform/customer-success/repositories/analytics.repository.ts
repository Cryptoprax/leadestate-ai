import"server-only";import type{SupabaseClient}from"@supabase/supabase-js";import type{CountryRecord,PlatformMetricRecord,RegionRecord}from"../types";export class AnalyticsRepository{constructor(private c:SupabaseClient){}async metrics(){const{data,error}=await this.c.from("platform_metrics").select("id,metric,value,unit,recorded_at").order("recorded_at",{ascending:false}).limit(100);if(error)throw error;return(data??[]).map(r=>({id:r.id,metric:r.metric,value:Number(r.value),unit:r.unit,recordedAt:r.recorded_at}))as PlatformMetricRecord[]}async alerts(){const{data,error}=await this.c.from("system_alerts").select("*").is("deleted_at",null).order("created_at",{ascending:false});if(error)throw error;return data??[]}async countries(){const{data,error}=await this.c.from("countries").select("id,code,name,status,country_head_id,version").is("deleted_at",null).order("name");if(error)throw error;return(data??[]).map(r=>({id:r.id,code:r.code,name:r.name,status:r.status,countryHeadId:r.country_head_id??undefined,version:r.version}))as CountryRecord[]}async regions(){const{data,error}=await this.c.from("regions").select("id,country_id,code,name,status,version").is("deleted_at",null).order("name");if(error)throw error;return(data??[]).map(r=>({id:r.id,countryId:r.country_id,code:r.code,name:r.name,status:r.status,version:r.version}))as RegionRecord[]}}
