import"server-only";import{HealthRepository}from"../repositories/health.repository";import{platformStaffContext}from"./platform-context";export class HealthService{private async repo(){const c=await platformStaffContext();return new HealthRepository(c.client)}async customers(){return(await this.repo()).customers()}async addNote(input:unknown){return(await this.repo()).addNote(input)}}
