import"server-only";import{ReleaseRepository}from"../repositories/release.repository";import{platformStaffContext}from"./platform-context";export class ReleaseService{private async repo(){const c=await platformStaffContext();return new ReleaseRepository(c.client)}async list(){return(await this.repo()).list()}async publish(input:unknown){return(await this.repo()).publish(input)}async archive(id:string,version:number){return(await this.repo()).archive(id,version)}}
