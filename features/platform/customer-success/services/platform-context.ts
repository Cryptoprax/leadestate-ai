import"server-only";import{createSupabaseServerClient}from"@/lib/supabase/server";export async function platformStaffContext(){const client=await createSupabaseServerClient(),{data:{user},error}=await client.auth.getUser();if(error||!user)throw new Error("Authenticated platform staff session required.");const role=String(user.app_metadata?.role??"");if(!["super_admin","platform_support"].includes(role))throw new Error("AtlasOS internal staff access required.");return{client,user,role}}
