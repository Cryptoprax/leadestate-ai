import { Button } from "@/features/platform/design-system";
import{approveSupportSessionAction,endSupportSessionAction}from"../actions/platform.actions";import type{SupportSession}from"../types";export function SupportActions({session}:{session:SupportSession}){if(!["pending","active"].includes(session.status))return null;const action=session.status==="pending"?approveSupportSessionAction:endSupportSessionAction;return <form action={action}><input type="hidden" name="sessionId" value={session.id}/><input type="hidden" name="expectedVersion" value={session.version}/><Button size="sm" variant={session.status==="pending"?"primary":"danger"}>{session.status==="pending"?"Approve access":"End session"}</Button></form>}
