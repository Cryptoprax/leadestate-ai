import { GitBranch, Link2, Network } from "lucide-react";
import type { CorrelationExploration } from "../../projections/live-contracts";

export function CorrelationExplorer({ exploration }: { readonly exploration?: CorrelationExploration }) { return <section className="rounded-3xl border border-vds-border/[.08] bg-vds-surface/[.02] p-5 sm:p-6"><div className="flex items-center gap-3"><Network className="size-5 text-vds-primary" aria-hidden="true"/><div><p className="text-xs uppercase tracking-[.17em] text-vds-primary">Correlation explorer</p><h2 className="mt-1 font-semibold">Event chain and object context</h2></div></div>{!exploration?.nodes.length ? <Empty text="Enter a correlation ID or select an event to reveal related events, objects, and causation without a graph dependency."/> : <div className="mt-6 grid gap-5 lg:grid-cols-[1fr_.7fr]"><ol className="space-y-2">{exploration.nodes.map(node => <li key={node.event.eventId} className="flex gap-3"><span className="mt-2 grid size-7 shrink-0 place-items-center rounded-full border border-vds-accent-border bg-vds-primary/[.07] text-[10px] text-vds-primary">{node.depth + 1}</span><div className="flex-1 rounded-xl border border-vds-border/[.07] p-3"><p className="text-sm">{node.event.summary}</p><p className="mt-1 text-[11px] capitalize text-vds-subtle">{node.relation} · {node.event.eventName}</p></div></li>)}</ol><div><h3 className="flex items-center gap-2 text-xs text-vds-muted"><Link2 className="size-4"/>Related objects</h3><div className="mt-3 space-y-2">{exploration.relatedObjects.map(object => <div key={`${object.objectType}:${object.objectId}`} className="rounded-xl border border-vds-border/[.07] p-3 text-xs"><span className="capitalize text-vds-secondary">{object.objectType}</span><span className="ml-2 text-vds-subtle">{object.displayHint ?? object.objectId}</span></div>)}</div><p className="mt-4 flex items-center gap-2 text-xs text-vds-subtle"><GitBranch className="size-3.5"/>Chain {exploration.complete ? "complete" : "has external causation"}</p></div></div>}</section> }
function Empty({ text }: { text: string }) { return <div className="mt-5 rounded-2xl border border-dashed border-vds-border p-8 text-center text-sm leading-6 text-vds-subtle">{text}</div> }
