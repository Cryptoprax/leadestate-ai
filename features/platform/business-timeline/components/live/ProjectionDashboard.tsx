import { CheckCircle2, DatabaseZap, RefreshCw, ScanLine } from "lucide-react";
import type { ProjectionHealth } from "../../projections/live-contracts";

export function ProjectionDashboard({ health }: { readonly health: readonly ProjectionHealth[] }) { return <section className="rounded-3xl border border-vds-border/[.08] bg-vds-surface/[.02] p-5 sm:p-6"><div className="flex items-center gap-3"><DatabaseZap className="size-5 text-vds-info" aria-hidden="true"/><div><p className="text-xs uppercase tracking-[.17em] text-vds-info">Projection dashboard</p><h2 className="mt-1 font-semibold">Readiness and sequence health</h2></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{health.map(item => <article key={item.projectionType} className="rounded-2xl border border-vds-border/[.07] bg-vds-input p-4"><div className="flex items-center justify-between"><h3 className="text-sm font-medium">{item.projectionType}</h3><span className="rounded-full border border-vds-border/[.08] px-2 py-1 text-[10px] capitalize text-vds-muted">{item.status}</span></div><div className="mt-4 space-y-2 text-xs text-vds-subtle"><p className="flex items-center gap-2"><RefreshCw className="size-3.5"/>Replay {item.replayReady ? "ready" : "not started"}</p><p className="flex items-center gap-2"><ScanLine className="size-3.5"/>Sequence {item.sequenceStatus}</p><p className="flex items-center gap-2"><CheckCircle2 className="size-3.5"/>Validation {item.validationStatus}</p></div></article>)}</div></section> }
