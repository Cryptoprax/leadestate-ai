import type { TimelineStore, TimelineValidationContext, TimelineValidator } from "../contracts/ports";
import type { AppendResult, EventProposal } from "../domain/contracts";
export class BusinessTimelineService { constructor(private readonly store: TimelineStore, private readonly validator: TimelineValidator) {} append(proposal: EventProposal, context: TimelineValidationContext): AppendResult { const validation = this.validator.validate(proposal, context); if (!validation.valid) throw new TimelineValidationFailure(validation.errors); return this.store.append(proposal) } correct(originalEventId: string, correction: EventProposal, context: TimelineValidationContext) { if (!this.store.get(originalEventId)) throw new Error("Original event not found."); return this.append({ ...correction, supersedesEventId: originalEventId }, context) } get(id: string) { return this.store.get(id) } replay(partition: string, from?: number) { return this.store.replay(partition, from) } }
export class TimelineValidationFailure extends Error { constructor(readonly errors: ReturnType<TimelineValidator["validate"]>["errors"]) { super("Business event validation failed.") } }
