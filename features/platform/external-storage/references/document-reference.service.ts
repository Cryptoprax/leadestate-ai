import type{DocumentReference}from"../domain/contracts";export class DocumentReferenceService{create(input:Omit<DocumentReference,"createdAt">):DocumentReference{return Object.freeze({...input,createdAt:new Date().toISOString()})}validate(reference:DocumentReference){if(!reference.externalFileId.trim()||!reference.objectId.trim())throw new Error("Document reference IDs are required.");return true}}
