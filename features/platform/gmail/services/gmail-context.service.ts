import type { GmailContextReference } from "../domain/models";
export class GmailContextService {references():readonly GmailContextReference[]{return Object.freeze([])} unavailableReason(){return "No authoritative CRM or Timeline link exists for this Gmail thread."}}
