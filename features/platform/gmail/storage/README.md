# Gmail storage boundary

Gmail message content and attachments are never persisted locally. The platform stores only the existing encrypted Google credential reference through the approved integration repository.
