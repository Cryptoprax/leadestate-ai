import { CircleDashed } from "lucide-react";
import type { ContextSectionViewModel } from "../view-models/contracts";
export function ContextSection({ section }: { readonly section: ContextSectionViewModel }) { return <section aria-labelledby={`context-${section.id}`} className="rounded-2xl border border-vds-border/[.07] bg-vds-surface/[.02] p-4"><div className="flex items-center gap-2"><span className={`size-1.5 rounded-full ${section.state === "available" ? "bg-vds-success" : "bg-vds-hover"}`}/><h3 id={`context-${section.id}`} className="text-sm font-medium">{section.title}</h3><span className="ml-auto text-[10px] uppercase tracking-[.12em] text-vds-subtle">{section.state.replace("-", " ")}</span></div>{section.items.length ? <ul className="mt-3 space-y-2">{section.items.map(item => <li key={item.id} className="rounded-xl border border-vds-border/[.06] bg-vds-input p-3"><p className="text-sm text-vds-secondary">{item.label}</p><p className="mt-1 text-[11px] text-vds-subtle">{item.meta}{item.occurredAt ? ` · ${item.occurredAt}` : ""}</p></li>)}</ul> : <div className="mt-4 rounded-xl border border-dashed border-vds-border/[.08] p-4 text-center"><CircleDashed className="mx-auto size-4 text-vds-subtle" aria-hidden="true"/><p className="mt-2 text-xs text-vds-muted">{section.message}</p></div>}</section> }
