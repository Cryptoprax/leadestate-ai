# AtlasOS Product Builder

AtlasOS owns all product configuration. Applications consume resolved configuration and never define navigation, modules, features, permissions, branding, themes, or settings. This sprint is an in-memory presentation architecture: services compose immutable configuration; hooks expose it to interactive previews; no persistence or business operations exist.

