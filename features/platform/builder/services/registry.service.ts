import{applications,brands,features,menus,modules,navigation,settings,themes,widgets}from"../config";import type{BuilderRegistry}from"../types";export function getBuilderRegistry():BuilderRegistry{return{applications,modules,features,navigation,menus,widgets,brands,themes,settings}}export function getApplicationComposition(applicationId:string){const application=applications.find(item=>item.id===applicationId);if(!application)return undefined;const installedModules=modules.filter(item=>application.moduleIds.includes(item.id));return{application,modules:installedModules,features:features.filter(item=>installedModules.some(module=>module.id===item.moduleId))}}
