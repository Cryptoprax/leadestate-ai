import"server-only";import{GmailService}from"@/features/platform/integrations/google/services/gmail.service";import type{UniversalBarResult}from"@/features/vayon/universal-bar/domain/contracts";
export class GmailUniversalSearchProvider{readonly id="gmail-live";async search(query:string,limit=10):Promise<readonly UniversalBarResult[]>{if(!query.trim())return[];const page=await new GmailService().list("inbox",query);return page.messages.slice(0,limit).map(message=>({id:`gmail-${message.id}`,label:message.subject||"(no subject)",description:`${message.sender.email} · ${message.snippet}`,href:`/vayon/email/${message.id}`,scope:"communications",kind:"record",keywords:[message.sender.email,...message.to.map(item=>item.email),...message.labels,...message.attachments.map(file=>file.name),message.threadId]}))}}
