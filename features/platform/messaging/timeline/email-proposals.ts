export const emailTimelineMappings=Object.freeze({received:"email.received",sent:"email.sent",draftSaved:"email.draft.saved",draftCreated:"email.draft.created",draftUpdated:"email.draft.updated",draftDeleted:"email.draft.deleted",deleted:"email.deleted",linked:"email.linked",attachmentAdded:"email.attachment.added",archived:"email.archived",labeled:"email.labeled"}as const);
export interface EmailTimelineProposal{readonly eventType:(typeof emailTimelineMappings)[keyof typeof emailTimelineMappings];readonly workspaceId:string;readonly messageId:string;readonly threadId:string;readonly occurredAt:string;readonly objectReferences:readonly {type:string;id:string}[]}
export function proposeEmailEvent(input:EmailTimelineProposal){return Object.freeze({...input,objectReferences:Object.freeze([...input.objectReferences])})}
