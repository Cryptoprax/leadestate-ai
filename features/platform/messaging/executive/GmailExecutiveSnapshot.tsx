import{GmailService}from"@/features/platform/integrations/google/services/gmail.service";
type Snapshot={status:"connected"|"not_connected"|"unavailable";unread?:number;awaiting?:number;today?:number};
async function load():Promise<Snapshot>{const service=new GmailService();try{if(await service.status()!=="connected")return{status:"not_connected"};const[unread,awaiting,today]=await Promise.all([service.list("unread"),service.list("inbox","-from:me"),service.list("inbox","newer_than:1d")]);return{status:"connected",unread:unread.resultSizeEstimate,awaiting:awaiting.resultSizeEstimate,today:today.resultSizeEstimate}}catch{return{status:"unavailable"}}}
export async function GmailExecutiveSnapshot(){const data=await load();if(data.status!=="connected")return <section className="rounded-2xl border border-vds-border p-5"><h2 className="font-semibold">Communication snapshot</h2><p className="mt-2 text-sm text-vds-muted">{data.status==="not_connected"?"Gmail is not connected.":"Gmail data is temporarily unavailable."}</p></section>;return <section className="rounded-2xl border border-vds-border p-5"><h2 className="font-semibold">Gmail snapshot</h2><dl className="mt-4 grid grid-cols-2 gap-4 text-sm sm:grid-cols-4"><div><dt className="text-vds-subtle">Unread emails</dt><dd className="mt-1 text-xl font-semibold">{data.unread}</dd></div><div><dt className="text-vds-subtle">Awaiting replies</dt><dd className="mt-1 text-xl font-semibold">{data.awaiting}</dd></div><div><dt className="text-vds-subtle">Today&apos;s emails</dt><dd className="mt-1 text-xl font-semibold">{data.today}</dd></div><div><dt className="text-vds-subtle">Mailbox</dt><dd className="mt-1 font-semibold text-vds-success">Connected</dd></div></dl><p className="mt-3 text-xs text-vds-subtle">Live Gmail estimates. Awaiting replies currently represents received inbox mail not sent by the connected account.</p></section>}
