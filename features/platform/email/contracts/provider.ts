import type{EmailAddress,EmailContent,EmailHealth,EmailProviderId}from"../types";export interface EmailDelivery{organizationId:string;workspaceId:string;messageId:string;from:EmailAddress;to:EmailAddress[];replyTo?:EmailAddress;content:EmailContent;tags:Record<string,string>}export interface EmailDeliveryResult{providerMessageId:string;acceptedAt:string}export interface TransactionalEmailProvider{readonly id:EmailProviderId;readonly version:string;send(input:EmailDelivery):Promise<EmailDeliveryResult>;health():Promise<EmailHealth>}
