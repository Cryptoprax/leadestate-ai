import"server-only";import{createCipheriv,createDecipheriv,createHash,randomBytes}from"node:crypto";const key=()=>{const secret=process.env.EMAIL_PAYLOAD_ENCRYPTION_KEY;if(!secret||secret.length<32)throw new Error("EMAIL_PAYLOAD_ENCRYPTION_KEY must contain at least 32 characters.");return createHash("sha256").update(secret).digest()};export function sealEmailVariables(value:Record<string,string|number|boolean>){const iv=randomBytes(12),cipher=createCipheriv("aes-256-gcm",key(),iv),encrypted=Buffer.concat([cipher.update(JSON.stringify(value),"utf8"),cipher.final()]);return Buffer.concat([iv,cipher.getAuthTag(),encrypted]).toString("base64url")}export function openEmailVariables(value:string){const payload=Buffer.from(value,"base64url"),iv=payload.subarray(0,12),tag=payload.subarray(12,28),encrypted=payload.subarray(28),decipher=createDecipheriv("aes-256-gcm",key(),iv);decipher.setAuthTag(tag);return JSON.parse(Buffer.concat([decipher.update(encrypted),decipher.final()]).toString("utf8"))as Record<string,string|number|boolean>}
