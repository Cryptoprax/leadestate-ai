import assert from "node:assert/strict";import{readFileSync}from"node:fs";import test from"node:test";
const read=path=>readFileSync(new URL(`../${path}`,import.meta.url),"utf8");
test("configuration engine exposes required builders",()=>{const page=read("features/leadestate/configuration/components/ConfigurationEngine.tsx");for(const component of["PipelineEditor","CustomFieldEditor","FormCanvas","WorkflowCanvas","PermissionMatrix","PreferencePanel","ConfigurationShell"])assert.match(page,new RegExp(component))});
test("configuration persistence is local and schema independent",()=>{const service=read("features/leadestate/configuration/services/configuration.service.ts");assert.match(service,/localStorage/);assert.doesNotMatch(service,/supabase|\.from\(|\.rpc\(/i)});
test("workflow model includes every foundation node",()=>{const types=read("features/leadestate/configuration/types.ts");for(const node of["trigger","condition","action","delay","notification","ai"])assert.match(types,new RegExp(`\\"${node}\\"`))});
