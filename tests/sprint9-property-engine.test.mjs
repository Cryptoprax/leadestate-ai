import test from"node:test";import assert from"node:assert/strict";import{existsSync,readFileSync}from"node:fs";const read=p=>readFileSync(p,"utf8");test("property routes are complete",()=>{for(const p of["app/vayon/properties/page.tsx","app/vayon/properties/new/page.tsx","app/vayon/properties/[propertyId]/page.tsx","app/vayon/properties/[propertyId]/edit/page.tsx"])assert.ok(existsSync(p),p)});test("writes use atomic RPCs and scoped reads",()=>{const repo=read("features/vayon/property/repositories/property.repository.ts");assert.match(repo,/\.rpc\("create_property"/);assert.match(repo,/\.eq\("organization_id"/);assert.match(repo,/\.eq\("workspace_id"/);assert.match(repo,/\.is\("deleted_at",null\)/)});test("migration enforces RLS soft delete and agent assignment",()=>{const sql=read("database/002_sprint9_property_engine.sql");assert.match(sql,/enable row level security/);assert.match(sql,/deleted_at/);assert.match(sql,/v_property\.assigned_agent_id=v_user/);assert.doesNotMatch(sql,/delete from properties/i)});test("catalogs contain required values",()=>{const config=read("features/vayon/property/config/catalogs.ts");for(const value of["available","reserved","sold","rented","under_negotiation","off_market","archived","apartment","villa","warehouse","farm_house"])assert.match(config,new RegExp(`"${value}"`))})
