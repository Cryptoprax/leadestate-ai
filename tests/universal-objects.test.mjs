import assert from"node:assert/strict";import{readFileSync}from"node:fs";import test from"node:test";const read=path=>readFileSync(new URL(`../${path}`,import.meta.url),"utf8");
test("universal object models expose canonical platform concepts",()=>{const models=read("features/platform/universal-objects/domain/models.ts");for(const concept of["UniversalContact","UniversalCompany","UniversalAddress","UniversalDocument","UniversalActivity","UniversalNote","UniversalAttachment","UniversalTag","UniversalRelationship"])assert.match(models,new RegExp(concept))});
test("universal object storage is schema independent",()=>{const storage=read("features/platform/universal-objects/storage/local.store.ts");assert.match(storage,/localStorage/);assert.doesNotMatch(storage,/supabase|\.rpc\(|\.from\(/i)});
test("global search covers required object scopes",()=>{const overlay=read("features/platform/universal-objects/components/SearchOverlay.tsx");for(const scope of["contact","company","property","lead","deal","task","calendar","document","note","tag","ai-memory"])assert.match(overlay,new RegExp(`\\"${scope}\\"`));assert.match(overlay,/ctrlKey|metaKey/);assert.match(overlay,/key\.toLowerCase\(\)===\"k\"/)});
test("AI integration remains a placeholder contract",()=>{const ai=read("features/platform/universal-objects/ai/contracts.ts");assert.match(ai,/PlaceholderUniversalObjectAIProvider/);assert.doesNotMatch(ai,/openai|anthropic|generateText/i)});
